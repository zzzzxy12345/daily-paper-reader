---
title: Reinforcement Learning for Computer-Use Agents with Autonomous Evaluation
title_zh: 具有自主评估能力的计算机使用智能体的强化学习
authors: "Marta Sumyk, Oleksandr Kosovan"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24515v1"
tags: ["query:agent"]
score: 9.0
evidence: 为计算机使用代理提供了基于自主评估的强化学习微调框架
tldr: 计算机使用智能体在图形界面中执行任务，但强化学习因缺乏可扩展的奖励信号而困难。本文提出利用视觉语言模型作为自主评估器，根据最终截图和指令判断任务完成度，提供噪声二进制奖励，并设计噪声校正估计器优化PPO训练。在macOSWorld、Windows Agent Arena和OSWorld三个基准上，校正后的奖励分别将成功率平均提升12.6个百分点（相对于零样本）和5.1个百分点（相对于原始奖励）。结果表明，显式建模并校正评估器噪声可使自主评估成为实用的RL奖励信号。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24515v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24515v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1516, \"height\": 722, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24515v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1633, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24515v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1646, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24515v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1047, \"height\": 275, \"label\": \"Table\"}]"
motivation: 强化学习训练计算机使用智能体面临开放环境中奖励信号难以获取的问题。
method: 利用视觉语言模型作为自主评估器，将反馈建模为噪声二进制奖励，通过噪声校正估计器优化PPO。
result: "在三个桌面环境基准上，校正奖励方法平均提升成功率12.6%（vs零样本）和5.1%（vs原始奖励）。"
conclusion: 显式建模评估器噪声后，自主评估可作为GUI环境中实用的RL奖励信号。
---

## 摘要
计算机使用智能体通过直接感知并操作图形用户界面来执行高级用户目标。然而，针对CUAs的强化学习仍然困难，因为开放的桌面环境很少提供可扩展的、机器可读的奖励信号：任务成功通常依赖于视觉信息，且难以通过手工设计的奖励函数或密集的人工标注来指定。我们提出了一种RL微调框架，该框架利用自主视觉-语言评估作为GUI智能体的可扩展监督信号。给定最终截图和原始指令，视觉-语言模型判断任务完成情况并提供终端反馈，无需在策略优化过程中使用特定任务的启发式规则或人工标注。由于自主评估器不完美，我们将其反馈建模为有噪声的二元奖励通道，并推导出用于近端策略优化的噪声校正奖励估计器。在macOSWorld、Windows Agent Arena和OSWorld上的实验表明，校正后的评估器奖励优于零样本基线和原始评估器奖励，成功率的提升幅度平均比零样本性能高12.6个百分点，比原始评估器微调高5.1个百分点。这些结果表明，当明确建模并校正评估器噪声时，自主评估可以作为GUI环境中RL的实用奖励信号。

## Abstract
Computer-Use Agents (CUAs) execute high-level user goals by perceiving and acting directly within graphical user interfaces. However, reinforcement learning for CUAs remains difficult because open-ended desktop environments rarely provide scalable, machine-readable reward signals: task success is often visually grounded and hard to specify with handcrafted reward functions or dense manual labels.   We propose an RL fine-tuning framework that uses autonomous vision-language evaluation as a scalable supervision signal for GUI agents. Given a final screenshot and the original instruction, a Vision-Language Model judges task completion and provides terminal feedback without task-specific heuristics or manual labels during policy optimization.   Because autonomous evaluators are imperfect, we model their feedback as a noisy binary reward channel and derive a noise-corrected reward estimator for Proximal Policy Optimization. Experiments across macOSWorld, Windows Agent Arena, and OSWorld show that corrected evaluator rewards outperform both zero-shot baselines and raw evaluator rewards, improving success rates by an average of 12.6 percentage points over zero-shot performance and 5.1 points over raw evaluator fine-tuning. These results suggest that autonomous evaluation can serve as a practical reward signal for RL in GUI environments when evaluator noise is explicitly modeled and corrected.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **背景**：计算机使用智能体（CUA）通过视觉感知图形界面执行自然语言任务，但强化学习（RL）训练面临关键瓶颈——开放桌面环境缺乏可扩展、机器可读的奖励信号，任务成功通常依赖视觉信息且难以用规则或密集标注指定。
- **核心问题**：如何为CUA的RL微调提供可靠、可扩展的奖励信号，同时避免人工标注和任务特定启发式。
- **整体含义**：本文提出利用视觉-语言模型（VLM）作为自主评估器（Judge），根据最终截图和指令判断任务完成度，输出二元噪声信号；通过显式建模并校正评估器噪声，使该信号可作为RL的有效奖励，从而推动CUA从交互中学习，无需人工标注。

### 2. 论文提出的方法论
- **核心思想**：将VLM评估器建模为有噪二元奖励通道，利用校准集估计其错误率，并推导噪声校正的奖励估计器，将其集成到PPO中进行策略优化。
- **关键技术细节**：
  - **任务建模**：将每个自然语言指令对应的交互过程建模为马尔可夫决策过程，终端奖励（0/1）由VLM评估器提供。
  - **噪声模型**：定义假阴性率 \( e_+ = P(\tilde{r}=0 | r^*=1) \) 和假阳性率 \( e_- = P(\tilde{r}=1 | r^*=0) \)，分离常数 \( D = 1 - e_+ - e_- > 0 \)（保证评估器优于随机猜测）。
  - **校正估计器**：基于条件期望推导无偏估计 \( \hat{r}_i = (\tilde{r}_i - e_-)/D \)，满足 \( E[\hat{r}_i | \tau_i] = r_i^* \)（真值）。
  - **实际实现**：在独立校准集上估计 \( \hat{e}_+ , \hat{e}_- \)，代入得到 \( \hat{r}_i^\sharp = (\tilde{r}_i - \hat{e}_-)/\hat{D} \)，其渐进无偏。
  - **策略优化**：将终端校正奖励向后传播计算优势函数，使用PPO更新策略；校准集与RL微调集严格分离，防止信息泄露。
- **算法流程**（文字说明）：
  1. 构建多OS任务数据集，划分为校准集（1/3）和RL微调集（2/3）。
  2. 在校准集上收集UI-TARS基座模型的运行轨迹，并由VLM评估器给出判断，对照人工标注计算 \( \hat{e}_+, \hat{e}_- \)。
  3. 对RL微调集中的每个任务，CUA与环境交互产生轨迹，终端截图送入VLM得到原始评估 \( \tilde{r} \)。
  4. 利用校准的误差率计算校正奖励 \( \hat{r}^\sharp \)。
  5. 使用PPO更新策略参数，以最大化校正奖励的期望折扣回报。

### 3. 实验设计
- **使用的数据集/场景**：
  - **自建数据集**：覆盖macOS/Windows/Linux共42个应用，每个应用60个任务（总计7560任务），拆分为校准（20任务）、RL微调（20+20任务）。
  - **外部任务描述**：融入OmniAct、GUI-World、GUIDE三个公开数据集的任务指令（仅用自然语言描述，不包含原始轨迹）。
- **Benchmark**：
  - macOSWorld（macOS）、Windows Agent Arena（Windows）、OSWorld（Linux）三个真实桌面环境。
- **对比方法**：
  - **基线**：零样本UI-TARS基座模型。
  - **RL变体**：
    - + 原始评估器奖励（\( \tilde{r} \)），统一模型（跨OS训练） vs. 每OS模型。
    - + 校正奖励（\( \hat{r} \)），统一模型 vs. 每OS模型。
- **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
- **未明确说明**。论文仅提及实验在三个桌面环境运行，未给出GPU型号、数量、训练时长等具体信息。文中指出“大规模交互数据”但未量化计算开销。这在工业级强化学习中是一个重要缺失。

### 5. 实验数量与充分性
- **主要实验**：3个基准 × 5种配置（零样本、原始统一/每OS、校正统一/每OS）= 15组结果。
- **额外分析**：
  - 评估器混淆矩阵（表2）：展示每OS的假阳/假阴率。
  - 消融实验：比较per-OS与统一模型训练的差异。
  - 任务多样性：覆盖42个应用×3 OS，并从三个外部数据集引入任务描述。
- **充分性与公平性**：
  - 实验设计全面，既对比原始评估器也校正版本，既检查统一也检查专有模型。
  - 校准集与训练集严格分离，避免信息泄露。
  - 局限性分析中主动讨论非平稳性、稀疏奖励、校准集质量、分布偏移等不足，说明实验认识客观。
  - 但缺乏与使用人工标注奖励或任务特定启发式的对比（如DOM检查或字符串匹配），这些在相关工作中有提及但未被纳入实验。

### 6. 论文的主要结论与发现
- **自主评估可作为RL的实用奖励信号**，前提是显式建模并校正评估器噪声。
- **校正奖励显著优于原始评估器奖励**：平均提升成功率12.6个百分点（vs零样本）和5.1个百分点（vs原始评估器）。
- **per-OS专有模型优于统一模型**：在三个基准上per-OS校正奖励均取得最佳成绩（macOSWorld: 0.203, Windows: 0.442, OSWorld: 0.432）。
- **原始评估器奖励效果不稳定**：虽然能提升部分任务，但在某些场景（如OSWorld）下甚至可能降低泛化能力。

### 7. 优点
- **方法创新**：将VLM评估器建模为噪声信道并推导无偏校正估计器，理论干净（渐进无偏），且可直接插入标准PPO，无需修改算法。
- **实用性强**：无需人工标注、无需任务特定启发式，完全依赖视觉信息通用监督，可实现规模化。
- **实验充分**：覆盖三个主流桌面OS、多种应用和外部数据集，结果清晰支持假设。
- **代码开源**：提供GitHub仓库，利于复现与扩展。

### 8. 不足与局限
- **实验覆盖局限**：
  - 未与其他RL奖励来源（如人工标注、程序化奖励）直接对比，难以量化校正奖励的绝对优势。
  - 未测试更多样的VLM模型（仅Qwen2-VL-7B），通用性存疑。
  - 缺少对分布偏移（新应用版本、不同分辨率、无障碍设置）的鲁棒性测试。
- **偏差风险**：
  - 错误率假设固定，但VLM性能可能随任务类型和UI状态变化，非平稳性会导致残余偏差。
  - 校准集质量直接影响修正效果，若 \( \hat{D} \) 接近零则噪声放大，可能引起训练不稳定。
  - 二元稀疏奖励导致探索困难，校正未增加密度，无法解决长时任务延迟满足问题。
- **应用限制**：
  - 依赖VLM视觉判断，存在盲点（如细微外观差异、替代解决方案）。
  - 当前方法仅提供终端奖励，缺乏中间进度信号或偏好反馈。
  - 未评估计算开销（VLM推理时间、PPO训练成本），可能与实时交互要求有差距。

（完）
