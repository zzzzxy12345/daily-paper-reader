---
title: Game-Theoretic Multi-Agent Reinforcement Learning for Swarm Trajectory Planning in Low-Altitude Wireless Networks
title_zh: 低空无线网络中基于博弈论的多智能体强化学习群体轨迹规划
authors: "Nguyen Duc Minh Quang, Ruoxi Chong, Zhiqiang Wei, Chang Liu, Derrick Wing Kwan Ng"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16386v1"
tags: ["query:agent"]
score: 9.0
evidence: 多智能体强化学习用于无人机群轨迹规划
tldr: 低空无线网络中多无人机轨迹规划面临资源块拥塞导致的策略耦合问题。现有方法忽略多小区耦合或仅适用于单小区场景，本文通过合作随机拥塞博弈建模，提出CTDE-MAPPO算法。该算法在集中训练分散执行框架下联合优化通信吞吐量与任务效率，仿真表明总效用和成功率均优于QMIX等基线，且收敛稳定。这一工作为大规模无人机编队在5G网络中的协同轨迹规划提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16386v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16386v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 900, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16386v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 811, \"height\": 535, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16386v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16386v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 233, \"label\": \"Table\"}]"
motivation: 现有研究忽略多小区资源块拥塞对无人机轨迹策略的耦合影响，无法适应大规模蜂窝连接场景。
method: 将问题建模为合作随机拥塞博弈，设计通信与任务感知的效用函数，采用CTDE-MAPPO算法实现集中训练与分散执行。
result: 相比QMIX、独立Q学习等基线，所提算法在总效用和任务成功率上显著提升，且在训练预算内稳定收敛。
conclusion: CTDE-MAPPO能有效解决多小区资源拥塞下的无人机轨迹规划问题，提升低空无线网络整体性能。
---

## 摘要
低空经济正在快速扩张，催生了低空无线网络，其中大规模蜂窝连接的无人机部署支持在多蜂窝地面基站基础设施上的异构关键任务应用。为确保任务成功，每架无人机必须联合优化通信吞吐量和任务完成效率。在第五代新空口系统中，等资源块分配策略导致无人机轨迹之间的强策略耦合：当无人机进入一个基站小区时，会减少所有同服务无人机的资源块份额，从而通过共享无线资源改变它们的可达速率和轨迹激励。现有研究要么忽略这种耦合，要么聚焦于单小区基础设施，导致多小区、感知拥塞的无人机轨迹规划问题未得到充分解决。为填补这一空白，我们将问题建模为具有通信和任务感知效用函数的合作随机拥塞博弈，并提出一种集中训练-分散执行的多智能体近端策略优化算法，以在多小区资源块拥塞下最大化社会福利。仿真结果表明，所提方法在总效用和任务成功率方面优于QMIX、独立Q学习和随机基线，同时在实际训练预算内实现稳定收敛。

## Abstract
The Low-Altitude Economy (LAE) is rapidly expanding, giving rise to low-altitude wireless networks (LAWNs), where large-scale cellular-connected unmanned aerial vehicle (UAV) deployments support heterogeneous mission-critical applications over multi-cell ground base station (GBS) infrastructures. To ensure mission success, each UAV must jointly optimize communication throughput and mission completion efficiency. In fifth-generation (5G) new radio (NR) systems, the equal resource block (RB) allocation policy induces strong strategic coupling among UAV trajectories: when a UAV enters a GBS cell, it reduces the RB share available to all co-served UAVs, thereby altering their achievable rates and trajectory incentives through shared wireless resources. Existing studies either ignore this coupling or focus on single-cell infrastructure, leaving the multi-cell, congestion-aware UAV trajectory planning problem insufficiently addressed. To fill this gap, we formulate the problem as a cooperative stochastic congestion game with a communication-and-mission-aware utility function, and propose a centralized-training decentralized-execution multi-agent proximal policy optimization (CTDE-MAPPO) algorithm to maximize social welfare under multi-cell RB congestion. Simulation results show that the proposed method outperforms QMIX, independent Q-learning, and random baselines in terms of aggregate utility and mission success rate, while achieving stable convergence within practical training budgets.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在低空无线网络（LAWN）中，多架蜂窝连接的无人机（UAV）共享多小区地面基站（GBS）的无线资源。由于5G NR系统采用等资源块（RB）分配策略，一架无人机进入某个小区会减少该小区内所有其他无人机可用的RB数，从而降低其可达速率。这种通过共享RB产生的策略耦合被现有研究忽略或仅考虑单小区场景，导致多小区、拥塞感知的无人机群轨迹规划问题尚未解决。
- **研究动机**：低空经济快速扩张，大规模无人机需要同时优化通信吞吐量和任务完成效率。现有方法要么忽略RB共享效应（如独立假设），要么只适用于单小区基础设施，无法处理多小区下RB拥塞引起的非平稳性。
- **整体含义**：本文首次将问题建模为**合作随机拥塞博弈**，并提出**CTDE-MAPPO**算法，能够在集中训练阶段利用全局拥塞信息学习协作策略，在分散执行阶段仅使用本地观测，实现高效、可扩展的轨迹规划。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 将多无人机轨迹规划问题建模为合作随机拥塞博弈 \( G = \langle \mathcal{N}, \mathcal{S}, \{\mathcal{A}_i\}, P, \{U_i\} \rangle \)，其中每架无人机的效用受其他无人机小区关联决策影响。
  - 通过**集中训练-分散执行（CTDE）** 框架，结合**多智能体近端策略优化（MAPPO）** 求解该博弈。中央评论家在训练时观察全局状态（包含各小区负载 \( n_b(t) \)），使策略梯度能正确分配信用；分散的行动者在执行时仅依赖本地观测 \( o_i(t) \)，无需智能体间通信。

- **关键技术细节**：
  - **系统模型**：
    - 每个GBS有 \( K_b \) 个RB，等分给关联无人机：\( \kappa_{b,i}(t) = \lfloor K_b / n_b(t) \rfloor \)。
    - 到达速率 \( R_i(t) = \kappa_{b_i,i}(t) \cdot \Delta f \log_2(1 + \text{SINR}) \)。
    - 时空离散化：3D空域划分为体素网格，时间离散为时隙。
  - **效用函数**：
    - 采用指数移动平均（EMA）速率 \( \bar{R}_i(T_i) \)（偏置校正后）与任务完成效率 \( T_i^{\min}/T_i \) 的乘积：
      \[
      U_i = \hat{R}_i(T_i) \cdot \frac{T_i^{\min}}{T_i}
      \]
    - 目标：最大化社会福利 \( W(S) = \sum_i U_i \)。
  - **MDP建模**：
    - 全局状态 \( x(t) \) 包含所有无人机位置、目的地、EMA速率及所有GBS负载。
    - 局部观测 \( o_i(t) \) 仅包含自身信息和全局负载 \( \{n_b(t)\} \)（作为拥塞充分统计量）。
    - 动作空间：3D位移（\( \|a\|_1 \leq v_{\max} \)），包括悬停。
  - **CTDE-MAPPO算法**：
    - **奖励设计**：每步奖励分解为速率变化奖励 \( r_i^{\text{rate}} \)、导航进展奖励 \( r_i^{\text{nav}} \)、碰撞惩罚 \( r_i^{\text{col}} \)、终止奖励 \( r_i^{\text{term}} \)。其中速率变化奖励与最终效用对齐，提供密集学习信号。
    - **网络结构**：行动者网络（共享参数MLP，2层256单元）、评论家网络（MLP，2层256单元）。评论家输入全局状态，输出期望团队回报。
    - **训练**：采用GAE估计优势，PPO clipped surrogate更新行动者，MSE损失更新评论家，并加入熵正则化。训练流程包括收集阶段（\( L \)步）和更新阶段（\( K \)个epoch），重复迭代。
    - **执行**：只部署行动者网络，每架无人机根据本地观测选择动作，无通信。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **实验场景**：
  - 模拟环境：4个GBS固定于正四边形网格，4架无人机起点和终点随机生成（无重叠）。空域为100×100×30体素网格，分辨率10m。最大时隙数 \( T_{\max}=100 \)，每时隙最大位移3体素。每GBS拥有10个RB，子带宽180 kHz，路径损耗指数2.0，LoS模型。
  - 训练时每次随机重置起点/终点，共训练约200万环境步。
- **对比方法**：
  1. **Random**：每时隙随机选择动作（非学习下限）
  2. **Independent Q-learning (IQL)**：每架无人机独立训练Q网络，仅用本地观测
  3. **QMIX**：基于值分解的集中训练分散执行方法，使用单调混合网络
- **评估指标**：
  - 平均每回合奖励（smoothed）
  - 任务成功率（到达目的地的无人机比例）
  - 测试阶段（200个episode，无探索）：平均累计奖励 \( \bar{J} \)、EMA校正速率 \( \hat{R} \)、平均完成时间 \( \bar{T} \)、成功率
  - 单智能体效用 \( U_i \) 的累积分布函数（CDF）

## 4. 资源与算力

- **文中未明确说明**：使用的GPU型号、数量、训练时长、内存等硬件信息。只提到训练历时约200万环境步（2×10^6 steps）。从算法复杂度判断，CTDE-MAPPO需要评论家输入全局状态，但未提及实际训练时间或资源需求。

## 5. 实验数量与充分性

- **实验数量**：
  - 训练过程在5个随机种子下进行，结果取平均。
  - 测试阶段使用200个独立测试episode（无探索）。
  - 对比了3个基线加上Random，共4种方法。
  - 提供了训练收敛曲线（图2）和测试表格（Table II）以及效用分布（图3）。
- **充分性评估**：
  - **正面**：多随机种子保证统计稳定性；测试episode数量充足；对比的基线覆盖了非学习、独立学习和值分解方法，具有代表性。
  - **不足**：
    - 未进行消融实验（如去掉速率奖励或导航奖励的影响）。
    - 未在不同规模场景（不同GBS数、不同UAV数）下测试。
    - 未对比其他MARL方法（如MADDPG、VDN、COMA等）。
    - 缺乏对超参数（如平滑因子β、GAE参数λ、学习率等）的敏感性分析。
    - 未讨论实际部署中的通信延迟、计算开销等问题。
  - **公平性**：基线实现合理，但未详细说明各基线的超参数是否经过调优。

## 6. 论文的主要结论与发现

1. **CTDE-MAPPO收敛快速且稳定**：在约150K环境步内成功率达到95%以上，后续保持稳定；相比之下QMIX收敛较慢且成功率波动，IQL非平稳性导致性能退化。
2. **CTDE-MAPPO获得最优测试性能**：
   - 平均累计奖励 \( \bar{J}=3.19 \)（远高于QMIX的3.01、IQL的-1.96、Random的-3.57）。
   - 任务成功率100%，完成时间最短（8.7 slots），同时保持较高通信速率（35.68 Mbps）。
3. **IQL的高速率是虚假的**：因其无人机倾向于悬停在起点附近轻负载小区，并未真正完成任务。
4. **社会最优导致效用分布不均**：CTDE-MAPPO产生的单智能体效用 \( U_i \) 分布方差较大，说明部分智能体通过更差路径（更拥挤）为集体利益牺牲个人效用；而QMIX分布更集中但均值更低。
5. **结论**：提出的博弈建模和CTDE-MAPPO能有效处理多小区RB拥塞下的轨迹规划，显著优于现有方法。

## 7. 优点：方法或实验设计上的亮点

- **问题建模创新**：首次将多小区等RB分配下的多无人机轨迹规划形式化为合作随机拥塞博弈，严格捕获了策略耦合。
- **效用函数设计合理**：采用EMA速率与任务效率的乘积，既考虑了通信质量的时间一致性，又鼓励快速到达，避免了单纯速率最大化导致的不合理长轨迹。
- **奖励分解巧妙**：利用EMA速率变化作为每步奖励，与终端效用保持一致性，解决稀疏回报问题。
- **CTDE框架的恰当选择**：集中评论家可以观察到全局负载，从而正确分配信用；分散执行仅需本地观测和全局负载广播（无需智能体间通信），符合实际部署约束。
- **实验分析深入**：除了平均性能，还分析了单智能体效用分布，揭示了社会最优的公平性权衡，为未来研究提供洞察。
- **代码和复现性**：论文详细描述了算法伪代码，参数表清晰，有助于复现。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅测试了4架UAV和4个GBS的小规模场景，未在更大规模（如数十架UAV、更多小区）下验证扩展性。
  - 缺少消融研究，无法量化各奖励项（如导航项、速率项）的贡献。
  - 未与更多MARL基线（如VDN、MADDPG、COMA等）对比。
- **模型假设较理想化**：
  - 假设空对地信道为纯LoS，且忽略小区间干扰（文中说明留作未来工作）。
  - 假设RB分配严格相等，实际5G中可能有更复杂的调度规则。
  - 假设无人机可实时获取全局负载 \( \{n_b(t)\} \)（虽然可通过广播实现，但延迟和可靠性未考虑）。
- **应用限制**：
  - 离散化的体素网格和动作空间可能限制实际UAV的灵活机动。
  - 训练需要集中收集全局状态，在机密性高或通信条件差的场景可能不可行。
  - 未考虑UAV能量约束（电池续航）。
- **公平性问题**：社会最优策略导致某些智能体效用极低，在真实任务中可能需要引入公平性约束。
- **未讨论收敛保证**：虽然仿真显示收敛，但缺乏理论证明。
- **资源/算力信息缺失**：未报告硬件环境，不利于复现和比较计算成本。

（完）
