---
title: A Multi-Agent system for Multi-Objective constrained optimization
title_zh: 面向多目标约束优化的多智能体系统
authors: Federica Filippini
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20236v1"
tags: ["query:agent"]
score: 9.0
evidence: 多智能体强化学习用于多目标约束优化
tldr: 在动态环境中，基于拉格朗日思想的强化学习方法通过加权惩罚项将成本和约束违规融合为单一奖励，但权重的手动选择难以适应非平稳场景。本文提出MAMO，利用多智能体强化学习将奖励权重选择转化为学习问题，实现任务执行与目标设计的解耦。该方法使智能体自主平衡主目标优化与约束违反规避，提升了鲁棒性和自主性，为动态环境下多目标约束优化提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20236v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 626, \"height\": 375, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20236v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 714, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20236v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 476, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20236v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 880, \"height\": 275, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20236v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 629, \"height\": 615, \"label\": \"Figure\"}]"
motivation: 手动选择奖励权重难以在动态环境中平衡成本优化与约束违反规避，亟需自动化方法。
method: 采用多智能体强化学习，将奖励权重选择作为学习问题，实现任务执行与目标设计解耦。
result: MAMO能够自动适应环境变化，自主平衡多目标约束优化中的权衡。
conclusion: MAMO为动态环境下更自主鲁棒的RL约束优化方案迈出第一步。
---

## 摘要
计算和网络系统中的许多决策问题可以自然地表示为在性能约束下的成本最小化问题。在动态环境中，强化学习（RL）通常通过在运行时将成本和违反约束的情况通过加权惩罚项嵌入到一个单一的标量奖励中（采用拉格朗日启发式的方法）来解决此类问题。然而，在这种背景下，学习到的策略的行为严重依赖于这些权重的选择，而权重通常需要手动挑选。这使得在优化主要目标和有效避免违反约束之间找到适当的权衡变得困难，特别是在非平稳环境中，它们的相对重要性可能发生变化。本文提出了MAMO（面向多目标约束优化的多智能体系统），一种通过多智能体RL来解决这种平衡问题的方法。MAMO通过将奖励权重的选择表述为一个学习问题，将任务执行与目标设计解耦，为在动态环境中实现更自主、更鲁棒的基于RL的约束优化解决方案迈出了第一步。

## Abstract
Many decision-making problems in computing and networking systems can be naturally formulated as cost-minimization problems under performance constraints. In dynamic environments, reinforcement learning (RL) is often used to solve such problems at runtime by embedding both costs and constraint violations into a single scalar reward through weighted penalty terms, following a Lagrangian-inspired formulation. However, in this context the behavior of the learned policy critically depends on the choice of these weights, which are typically selected manually. This makes it difficult to identify an appropriate trade-off between optimizing the primary objective and effectively avoiding constraint violations, particularly in non-stationary environments where their relative importance may change. This paper presents MAMO (Multi-Agent system for Multi-Objective constrained optimization), an approach to tackle this balancing problem through multi-agent RL. MAMO decouples task execution from objective design by formulating the selection of reward weights as a learning problem, providing a !rst step towards more autonomous and robust RL-based solutions for constrained optimization problems in dynamic environments.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在动态环境中，基于拉格朗日思想的强化学习方法通常通过手动选择的权重将成本优化和约束违反惩罚合并为单一奖励。这种权重的固定手动设定难以适应非平稳环境，导致策略要么过于保守（高成本），要么过于激进（频繁违反约束）。因此，需要一种自动、自适应的权重调节机制来平衡多目标约束优化中的权衡。
- **整体背景**：问题来源于计算与网络系统中的成本最小化问题（如云计算资源分配、边缘计算副本扩缩），需满足服务质量（QoS）约束。强化学习常用于运行时决策，但权重的手动选择成为瓶颈。论文旨在通过多智能体强化学习将权重选择本身也学习化，提升系统的自主性和鲁棒性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出 MAMO（Multi-Agent system for Multi-Objective constrained optimization），采用**分层多智能体架构**，将任务执行与目标设计解耦：
  - **Task-Execution (TE) 智能体**：直接与环境交互，学习控制策略。其奖励函数为标准加权和：\( r = 1 - \sum (w_0 \cdot \text{cost} + w_1 \cdot \text{constraint penalty}) \)，权重固定。
  - **Weight-Adaptation (WA) 智能体**：在更高层次、更慢时间尺度运行。不直接作用于环境，而是学习选择权重 \( w \) 的值，以引导TE智能体行为符合系统级目标（如满足约束同时最小化成本）。
- **工作流程**（迭代循环）：
  1. WA智能体选择一个权重（初始随机），固定给TE智能体。
  2. TE智能体在固定权重下训练一定周期（示例中15k步），学习对应策略。
  3. 训练结束后，WA智能体观察TE智能体的**长期性能指标**（平均执行成本、平均拒绝概率），若拒绝概率低于容忍阈值则奖励为成本（越低越好），否则奖励为0（惩罚）。
  4. WA智能体据此更新自身策略（RL算法），选择新的权重，开始下一轮迭代。
- **技术实现**：TE和WA智能体均使用**深度Q学习（Deep Q-Learning）**，网络结构为三层全连接 [256, 128, 256]，折扣因子0.7，学习率5e-4，优先经验回放缓冲区容量10240，目标网络每1000步更新，探索采用ε-贪婪策略（分段调度，与MAMO周期同步）。WA动作空间离散化为步长0.01。

## 3. 实验设计

- **场景**：简化版边缘FaaS（函数即服务）副本缩放问题，仅考虑单个函数，单个权重 \( w \) 对应拒绝概率惩罚，\( 1-w \) 对应成本。环境参数：最大副本数 \( N_f = 10 \)，冷启动时间1.0s，热启动0.1s，空闲60s后终止。容忍拒绝阈值 \( tol = 0.05 \)。
- **工作负载**：正弦轨迹模拟昼夜模式，并添加均匀噪声（0.9~1.1）以模拟非平稳性。
- **基准（Baseline）**：
  1. **离线最优解**：使用Gurobi求解原始问题（P1），假设完全知道负载 \( \lambda_f \)，得到理论下界。
  2. **扰动负载下的静态最优解**：对负载加噪声后仍采用离线最优解，观察实际违反情况。
  3. **固定权重极端情况**：分别训练TE智能体使用 \( w=0.99 \)（几乎只惩罚违规）和 \( w=0.1 \)（几乎只关心成本），展示极端行为。
- **对比方法**：本文未与其他自适应权重方法（如双分解、贝叶斯优化、多策略算法）进行对比，仅展示了MAMO自身的性能演变。
- **实验内容**：运行完整MAMO框架，初始 \( w=0.99 \)，TE训练15k步，WA观察最后300步的平均拒绝概率和成本，据此更新。训练过程中跟踪 \( w \) 收敛情况以及拒绝概率 \( p \) 的变化。

## 4. 资源与算力

- **文中未明确说明**：未提及使用的GPU型号、数量、训练总时长等算力信息。实验基于RL4CC库（基于Ray RLlib），通常可在普通单GPU或CPU上完成（场景规模小）。因此算力细节未知。

## 5. 实验数量与充分性

- **实验数量**：
  - 离线最优解曲线（图3）。
  - 扰动负载下静态解曲线（图3下方）。
  - 固定权重极端行为（图4：\( w=0.99 \) 和 \( w=0.1 \)）。
  - **完整MAMO仅一个实验**：图5展示WA训练过程中 \( p \) 与 \( w \) 的变化，以及最终一个代表性权重（\( w=0.85 \)）下的详细解。
- **充分性与公平性**：
  - **局限**：实验场景极为简化（单个函数、单个权重）、未对比任何其他自适应权重方法（如贝叶斯优化、双分解、多策略RL）、未在不同工作负载模式或不同约束条件下验证。仅有一组结果，缺乏统计显著性报告（如多次运行均值方差）。消融实验未做（如对比WA智能体不同算法、不同时间尺度）。
  - **客观性**：作者在结论部分承认这是“preliminary application”，并计划未来与更多方法对比。因此实验充分性较低，但作为初步验证，能展示MAMO的基本可行性。

## 6. 论文的主要结论与发现

- MAMO能够**自主收敛**到满足约束的权重配置（\( w \) 趋近0.8~0.9），且拒绝概率始终保持在容忍阈值0.05以下，同时成本接近但不能达到离线最优（略高）。
- 与固定极端权重相比，MAMO避免了过度保守或过度激进的行为，实现了自适应的权衡。
- 该方法证明了将权重选择作为学习问题的可行性，为动态环境下更自主、鲁棒的RL约束优化迈出第一步。

## 7. 优点

- **创新性**：将奖励权重的选择视为一个单独的学习问题，引入层次化多智能体结构，将任务执行与目标设计解耦，思路新颖。
- **自适应能力**：无需手动调参，WA智能体可随环境变化自动调整权重，适合非平稳场景。
- **兼容性**：不改变底层RL算法（TE智能体仍使用标准加权奖励），可与任意内层RL算法组合。
- **可解释性**：权重维度低（仅几个系数），WA智能体仅依赖全局性能摘要，结构清晰。

## 8. 不足与局限

- **实验覆盖不足**：仅测试单一函数场景，未扩展到多函数或多权重情况。
- **缺乏对比方法**：未与任何现有自适应权重方法（如双分解、贝叶斯优化、元梯度、多策略OLP等）进行定量比较，无法评估MAMO的相对优势。
- **统计可靠性**：未报告多次重复实验的均值和方差，单次运行结果可能存在偶然性。
- **收敛速度与计算开销**：未分析WA智能体所需的迭代次数和训练总时长，实际应用中的实时性可能受限。
- **偏差风险**：实验设计基于特定工作负载模式（正弦+噪声），在其他动态模式（如突发、周期变化）下的表现未知。
- **应用限制**：当前方法假设WA智能体可以访问全局性能摘要，可能不适用于完全去中心化或隐私敏感系统。

（完）
