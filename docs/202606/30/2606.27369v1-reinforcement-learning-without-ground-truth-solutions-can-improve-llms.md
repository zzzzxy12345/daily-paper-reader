---
title: Reinforcement Learning without Ground-Truth Solutions can Improve LLMs
title_zh: 无真实解强化学习可提升大型语言模型
authors: "Yingyu Lin, Qiyue Gao, Nikki Lijing Kuang, Xunpeng Huang, Kun Zhou, Tongtong Liang, Zhewei Yao, Yi-An Ma, Yuxiong He"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27369v1"
tags: ["query:rl-papers"]
score: 9.0
evidence: 利用确定性执行反馈作为连续奖励的无真实解RL框架
tldr: "强化学习训练LLM通常依赖真实答案来分配奖励，限制了在无真实解任务上的应用。本文提出RiVER框架，利用确定性执行反馈作为连续奖励，通过实例级比较和强调顶级解的校准方法解决尺度主导和频率主导问题。在12个AtCoder启发式竞赛任务上训练后，模型在ALE-Bench上评级提升8.9%和9.4%，并迁移至LiveCodeBench和USACO，分别提高2.4%和3.5%。这表明无需真实解的分数优化任务结合适当奖励校准可有效提升通用编码能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1694, \"height\": 841, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 608, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1687, \"height\": 957, \"label\": \"Table\"}]"
motivation: 真实解难获取时，传统RLVR受限；需利用执行反馈作为连续奖励，但存在尺度主导和频率主导问题。
method: 提出RiVER，使用确定性执行反馈作为连续奖励，通过实例级比较校准奖励，强调顶级解并限制其他解的上界。
result: "在ALE-Bench上Qwen3-8B和GLM-Z1-9B-0414评级提升8.9%和9.4%；在LiveCodeBench和USACO上绝对平均提升2.4%和3.5%。"
conclusion: 分数优化任务结合奖励校准可替代真实解，有效训练LLM的通用编码能力。
---

## 摘要
基于可验证奖励的强化学习（RLVR）训练大型语言模型通常依赖真实答案来分配奖励，这限制了其在真实解未知的任务中的应用。我们提出了一种**排序诱导可验证框架**（RiVER），该框架在无真实解的情况下，利用确定性执行反馈作为连续值监督，训练模型处理基于分数的优化任务。将组相对强化学习应用于此类连续奖励时，我们识别出两个关键挑战：*尺度主导*，即测试实例间未校准的分数幅度扭曲策略更新；以及*频率主导*，即重复采样的次优解可能压倒稀有但更强的候选解。RiVER通过校准奖励塑造来应对这些挑战，该方法利用实例比较并强调排名靠前的解，同时为其他有效解保留有界反馈。我们在12个AtCoder启发式竞赛任务上进行训练，并在算法工程基准（ALE-Bench）、LiveCodeBench和USACO上评估。RiVER将Qwen3-8B和GLM-Z1-9B-0414在ALE排名中分别提升了8.9%和9.4%。更重要的是，尽管仅基于分数任务训练且无任何真实解，RiVER在LiveCodeBench和USACO等精确解基准上，也将骨干模型的绝对平均性能分别提升了2.4%和3.5%。相比之下，使用原始执行分数训练的基线模型虽然提升了ALE排名，但未能迁移到精确解基准。这些结果表明，基于分数的优化任务结合适当的奖励校准，可以作为无真实解情况下通用编码能力的有效训练环境。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) for training LLMs typically rely on ground-truth answers to assign rewards, limiting their applicability to tasks where the ground-truth solution is unknown. We introduce a \textbf{R}anking-\textbf{i}nduced \textbf{VER}ifiable framework (RiVER) that trains LLMs on score-based optimization tasks without ground-truth solutions, using deterministic execution feedback as continuous-valued supervision. When applying group-relative RL to such continuous rewards, we identify two key challenges: \emph{scale dominance}, where uncalibrated score magnitudes across test instances distort policy updates, and \emph{frequency dominance}, where repeatedly sampled suboptimal solutions can outweigh rare but stronger candidates. RiVER addresses these challenges with calibrated reward shaping that uses instance-wise comparisons and emphasizes top-ranked solvers while retaining bounded feedback for other valid solutions. We train on 12 AtCoder Heuristic Contest tasks and evaluate on Algorithm Engineering Benchmark (ALE-Bench), LiveCodeBench, and USACO. RiVER advances Qwen3-8B and GLM-Z1-9B-0414 by 8.9\% and 9.4\% in ALE rating rank. More importantly, despite training exclusively on score-based tasks without any ground-truth solutions, RiVER also improves the backbones across exact-solution benchmarks such as LiveCodeBench and USACO by an absolute average improvement of 2.4\% and 3.5\%. By contrast, baselines trained with raw execution scores improve ALE rating but fail to transfer to exact-solution benchmarks. These results suggest that score-based optimization tasks, combined with proper reward calibration, can serve as effective training environments for general coding ability without ground-truth solutions.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）
- **传统 RLVR 的局限**：现有 RLVR（基于可验证奖励的强化学习）训练 LLM 时严重依赖 **真实解**（ground-truth）来分配二元奖励（正确/错误），限制了其在许多重要但无单一最优解的开放式推理和编程任务（如算法设计、优化、规划）上的应用。
- **研究动机**：探索是否可以在**完全无需真实解**的情况下，仅利用**确定性执行反馈**（如约束检查、目标函数值）作为训练信号来改进 LLM。这类任务通常属于 NP-hard 优化问题，最优解未知，但可行解可被执行和比较。
- **整体含义**：首次证明基于分数的优化任务（如 AtCoder 启发式竞赛）结合适当的奖励校准，可以替代真实解作为有效训练环境，不仅提升优化任务性能，还能**迁移到精确解编程基准**，为扩大 RLVR 适用范围提供新方向。

## 2. 方法论
### 2.1 核心思想
- 提出 **RiVER（Ranking-induced VERifiable framework）**，将无真实解的优化任务转化为强化学习信号：对每个问题采样一组可执行求解器，在同一组隐藏测试实例上运行，由确定性的任务评估器返回可行性和目标值；通过 **实例级排序** 和 **胜者重奖励塑造** 将原始分数转化为稳定策略梯度信号。
### 2.2 关键技术细节
- **反馈来源**：对于采样响应 \(o\)，提取代码 \(c = \text{Code}(o)\)，在 \(M\) 个隐藏实例上执行，得到可行性矩阵 \(V \in \{0,1\}^{G \times M}\) 和分数矩阵 \(F \in \bar{\mathbb{R}}^{G \times M}\)（无效执行记为 \(-\infty\)）。
- **实例级排序**：对每个隐藏实例 \(T_m\)，将 \(G\) 个求解器按分数排名（平均排序法处理平局），生成排名 \(r_{i,m} \in [1, G]\)。此步骤消除实例间分数尺度差异。
- **胜者重奖励塑造**：定义每个求解器-实例对的转化奖励 \(s_{i,m}\)：
  - 无效输出：\(-1\)
  - 最佳有效求解器（排名=1）：\(+1\)
  - 有效但非最佳：使用线性映射 \(\text{clip}((G-1 - r)/(G-3), 0, 1) - 0.5\)，奖励范围 \([-0.5, 0.5]\)
  - 这样，最佳解获得显著区分，非最佳解仍提供有界分级反馈，减少频率主导。
- **聚合与更新**：平均各实例奖励得到样本级优势 \(\bar{s}_i\)，直接作为 GRPO 中的优势 \(A_i\)，无需额外标准化。
### 2.3 公式与算法流程（文字说明）
- 使用 GRPO 框架，但用 RiVER 构造的 \(\bar{s}_i\) 替换传统二元或原始分数奖励。
- 目标函数：带裁剪和 KL 正则化的 GRPO 损失（公式 3）。
- 算法流程：对于每个提示 \(q\) → 从旧策略采样 \(G\) 个响应 → 提取代码并执行 → 计算实例级排名和胜者重奖励 → 平均得到优势 → 应用 GRPO 更新。

## 3. 实验设计
### 3.1 训练环境
- **训练集**：选自 AtCoder Heuristic Contest（AHC）中 AHC047–AHC062 的 16 个任务，排除 4 个不兼容的，保留 **12 个任务**用于训练。这些任务均为分数型优化问题，无真实解，有官方评估器和测试实例生成器。
### 3.2 评估基准
- **分数型基准**：**ALE-Bench**（基于 AHC 问题，评估优化导向算法工程能力），报告 Rating（越高越好）和 Rank%（越低越好）。
- **精确解基准**：**LiveCodeBench v5 & v6**（按 Easy/Medium/Hard 细分）和 **USACO**，按 Pass@1 平均准确率评估。
### 3.3 基线方法
- 使用两个骨干模型：**Qwen3-8B** 和 **GLM-Z1-9B-0414**。
- 对比五种奖励设计变体：
  1. **Raw-GRPO**：直接使用原始分数聚合后 GRPO。
  2. **RS-GRPO**：用风险敏感变换替代组标准化。
  3. **Raw-Binary**：最高分得 1，其余得 0。
  4. **Instance-Norm**：实例内标准化原始分数再聚合。
  5. **Rank-uniform**：实例级排名但给予均匀间隔奖励。
  6. **RiVER**（本文方法）。

## 4. 资源与算力
- 原文仅在训练细节中提及优化器为 AdamW，学习率 \(1 \times 10^{-6}\)，KL 系数 0.001，组大小 \(G=16\)，全局批量大小 64。
- **未明确说明**使用的 GPU 型号、数量或训练时长。因此无法量化算力消耗，属于论文的一项信息缺失。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验在两个骨干模型各 6 种方法（包括原始模型）下进行了完整评估（表 1），覆盖 3 个基准（ALE-Bench 含 Rating/Rank%，LiveCodeBench 含 3 个难度，USACO 为主结果）。
  - 额外展示了训练动态（图 2，12 个任务的最佳分数曲线）和案例研究（AHC057 代码对比）。
  - 信息论部分提供了理论分析（命题 6.2 证明）。
- **充分性与客观性**：
  - 基线覆盖全面，包括原始分数、标准化、二元、均匀排名等多种设计，消融充分。
  - 评估基准多样，既有分数型又有精确解，且 LiveCodeBench 按难度细分，能反映迁移性能。
  - 重复 3 次独立运行报告平均性能，降低了随机性影响。
  - 训练集与 ALE-Bench 无重叠，避免数据泄露。
  - 总体实验设计较为严谨，但缺少对更多骨干模型（如更大规模模型）的验证，以及不同组大小 G 的敏感性分析。

## 6. 主要结论与发现
1. **RiVER 显著提升分数型基准**：在 ALE-Bench 上，Qwen3-8B 评级提升 142 点（+8.9%），GLM-Z1-9B 提升 157 点（+9.4%）。
2. **迁移到精确解基准**：仅用优化任务训练，RiVER 在 LiveCodeBench 和 USACO 上分别带来绝对平均 2.4% 和 3.5% 的提升，且是所有方法中**唯一**在 Qwen3-8B 上所有五个指标均表现更好的方法。
3. **原始分数校准不足**：Raw-GRPO 等基线虽提升 ALE 排名，但无法一致提升精确解基准，表明直接优化原始分数易导致过拟合于分数分布。
4. **实例级排序 + 胜者重塑造是关键**：Instance-Norm 和 Rank-uniform 提升有限，加上胜者侧重后产生显著迁移收益，说明奖励形状对跨域泛化至关重要。
5. **困难问题收益更大**：LiveCodeBench 上 Medium 和 Hard 难度提升显著，Easy 接近饱和，表明训练优化任务有助于提升复杂推理能力。

## 7. 优点
- **创新性**：第一个系统性地利用无真实解的优化任务作为 RLVR 训练环境，并证明可迁移至精确解基准。
- **方法简洁有效**：RiVER 仅通过实例级排序和简单的胜者重奖励塑造，就解决了尺度主导和频率主导两大挑战，无需复杂模型或额外标注。
- **信息论解释**：提出组相对反馈分辨率（GFR）概念，从熵角度解释为什么分数型环境能提供更丰富的学习信号，与实证结果一致。
- **实验设计全面**：多个基线、多个骨干、多种评测基准，消融实验清晰，且训练与评测数据无重叠。
- **实际意义**：开辟了利用大量无真实解的优化竞赛数据训练 LLM 的途径，具有规模化潜力。

## 8. 不足与局限
- **计算资源信息不足**：未报告 GPU 型号、数量、训练时长等算力开销，难以评估方法可复现性和效率。
- **骨干模型有限**：仅测试 8B/9B 级模型，未涵盖更大规模或不同架构的 LLM，泛化性有待验证。
- **超参数敏感性未探讨**：组大小 G=16 固定，未研究 G 对学习效果的影响；KL 系数、学习率等也未进行更细粒度的调参分析。
- **领域局限性**：训练数据全部来自 AtCoder 启发式竞赛（同类型优化问题），结果是否可推广到其他领域（如数学证明、规划调度）未知。
- **无真实解验证的潜在风险**：奖励完全基于组内相对排序，如果组内所有解都很差，则最优解也可能被较高奖励，可能误导策略陷入局部次优。
- **迁移增益幅度较小**：在 LiveCodeBench 上平均提升约 2-3%，虽然一致但幅度有限，且在 Easy 问题上几乎无提升，表明对简单任务帮助不大。
- **未讨论失败案例分析**：虽然给出一个正例（AHC057），但未分析基线和 RiVER 在某些任务上表现持平或退步的情况。

（完）
