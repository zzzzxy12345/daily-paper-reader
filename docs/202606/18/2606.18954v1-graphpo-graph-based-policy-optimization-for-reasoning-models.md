---
title: "GraphPO: Graph-based Policy Optimization for Reasoning Models"
title_zh: GraphPO：基于图的策略优化用于推理模型
authors: "Yuliang Zhan, Xinyu Tang, Jian Li, Dandan Zheng, Weilong Chai, Jingdong Chen, Jun Zhou, Ge Wu, Wenyue Tang, Hao Sun"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18954v1"
tags: ["query:agent"]
score: 9.0
evidence: 使用图结构的推理模型强化学习策略优化
tldr: 现有强化学习验证奖励（RLVR）范式独立采样响应，导致冗余探索和稀疏奖励问题。GraphPO将推理过程构建为有向无环图，节点表示语义状态，边表示推理步骤，合并语义等价路径以共享后缀并重新分配预算。该方法对入边分配效率优势、出边分配正确性优势，理论降低优势估计方差。在三个LLM的推理和智能体搜索任务中，GraphPO以相同token预算一致超越链式和树式基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18954v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 731, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1446, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1431, \"height\": 867, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1452, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1442, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 513, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 513, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 519, \"height\": 366, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18954v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 1051, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18954v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18954v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 357, \"label\": \"Table\"}]"
motivation: 独立采样响应导致冗余探索，树方法分支间信息孤立且优势估计方差高，需更高效的策略优化框架。
method: 将rollout表示为有向无环图，合并语义等价路径为节点，对入边分配效率优势、出边分配正确性优势，从结果推导过程监督。
result: 在三个LLM（如Llama、Mistral）的推理和智能体搜索基准上，GraphPO以相同token或响应预算一致优于链式和树式基线。
conclusion: GraphPO通过图结构合并等价路径、降低方差，显著提升推理效率与策略优化质量，是推理模型训练的有效新范式。
---

## 摘要
带有可验证奖励的强化学习（RLVR）已成为增强大型推理模型能力的标准范式。RLVR通常独立采样响应，并基于最终答案优化策略。该范式存在两个局限：首先，独立响应常包含相似的中间推理步骤，导致冗余探索和计算浪费；其次，稀疏的最终答案奖励使得难以识别有效步骤。基于树的方法通过共享前缀并比较同一前缀下的分支来提供细粒度信号，从而部分解决了这一问题。然而，树的分支仍独立扩展。当不同分支达到相似的推理状态时，它们无法共享信息，会重复类似的探索。此外，基于树的方法忽略了这种分散性，仅在不同分支内进行局部比较，这可能导致优势估计的方差更高。为解决这一挑战，我们提出了GraphPO（基于图的策略优化），一种新颖的RL框架，将轨迹表示为有向无环图，以推理步骤作为边，从推理路径中总结出的语义状态作为节点。GraphPO将语义等价的推理路径合并为等价类，允许它们共享后缀，并将预算从冗余扩展重新分配到多样化探索。此外，我们为入边赋予效率优势，为出边赋予正确性优势，从而在从结果推导过程监督的同时提高推理效率。理论表明，GraphPO降低了优势估计方差并提升了推理效率。在三个大型语言模型上的推理和智能搜索基准实验表明，在相同令牌预算或响应预算下，GraphPO始终优于基于链和基于树的基线。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has become a standard paradigm for enhancing the capability of large reasoning models. RLVR typically samples responses independently and optimizes the policy using from final answers. This paradigm has two limitations. First, independently responses often contain similar intermediate reasoning steps, causing redundant exploration and wasted computation. Second, sparse final-answer rewards make it hard to identify useful steps. Tree-based methods partly address this problem by sharing prefixes and comparing branches from the same prefix to provide fine-grained signals. However, tree branches are still expanded independently. When different branches reach similar reasoning states, they cannot share information and repeat similar exploration. Moreover, tree-based methods ignore such dispersion and only perform local comparisons within separate branches, which can lead to higher variance in advantage estimation. To address this challenge, we propose GraphPO (Graph-based Policy Optimization), a novel RL framework that represents rollouts as a directed acyclic graph, with reasoning steps as edges and semantic states summarized from the reasoning paths as nodes. GraphPO merges semantically equivalent reasoning paths into equivalence classes, allowing them to share suffixes and reallocating budget away from redundant expansions to diverse exploration. Furthermore, we assign efficiency advantages to incoming edges and correctness advantages to outgoing edges, thereby improving inference efficiency while deriving process supervision from outcome. Theory shows that GraphPO reduces advantage-estimation variance and enhances reasoning efficiency. Experiments on three LLMs across reasoning and agentic search benchmarks show that GraphPO consistently outperforms chain- and tree-based baselines with the same token budgets or response budgets.

---

## 论文详细总结（自动生成）

# 论文《GraphPO: Graph-based Policy Optimization for Reasoning Models》详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：强化学习与可验证奖励（RLVR）已成为提升大型推理模型能力的主流范式。现有 RLVR 方法通常独立采样多条响应，基于最终答案的二元奖励优化策略。但独立采样导致大量相似的中间推理步骤重复出现，造成冗余探索和计算浪费；同时稀疏的最终奖励难以为中间步骤分配信用。
- **树方法的局限**：基于树的 RL 方法（如 TreeRL、SPO）通过共享前缀和分支比较提供了更细粒度的信号，但仍存在三个问题：
  1. 深层树扩展会增加冗余推理步骤，降低推理效率；
  2. 过早终止的路径提供有限训练信号，降低利用效率；
  3. 不同分支达到相似的推理状态时仍独立扩展，无法共享后续信息，导致冗余探索和更高的优势估计方差。
- **核心动机**：将推理轨迹建模为有向无环图（DAG），通过合并语义等价的中间状态，实现信息共享、降低方差、提升探索效率和推理效率。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将每次推理的 rollout 构建为一个 DAG，其中**节点**表示从初始 prompt 到该位置的语义状态（通过摘要提取），**边**表示生成的推理步骤。
- 检测不同路径中语义相似的节点，将它们合并为**等价类**（equivalence class），允许等价类内的节点共享后续后缀（suffix），并重新分配冗余扩展的预算到新状态。

### 关键技术细节

#### 图构建（Graph Construction）
- 逐层扩展：每层从当前活跃节点采样多条子边（推理步骤），对每个新节点提取语义摘要并计算嵌入向量（使用 SFR-Embedding-2-R）。
- 非因果节点对（不在祖先-后代关系中的节点）间计算余弦相似度，若 `sim(u, v) ≥ κ` 则合并到同一等价类。
- 当一条路径到达已存在的等价类时，其后续扩展预算减半（至少保留一条路径），节省的预算重新分配给其他活跃新状态，实现探索效率提升。

#### 图奖励（Graph Reward）
- 对每个节点 `v`，计算其自身得分 `S(v)`：基于从该节点通过直接边可达的终态的正确性分数，并加权池化等价类内其他节点的分数：
  ```
  S(v) = (c_own_v + w * Σ_{q∈Q(v)\{v}} c_own_q) / (t_own_v + w * Σ_{q∈Q(v)\{v}} t_own_q)
  ```
  其中 `w` 为池化系数，`R(z)∈{0,1}` 为验证器奖励。
- 步骤级奖励 `r_step(u,v) = (S(v) - S(u)) * (1 - η(u,v))`，其中 `η(u,v) = max(0, sim(u,v))`，用于抑制语义重复步骤的贡献。

#### 双组图优势（Dual-Group Graph Advantage）
- **正确性组（correctness group）**：对于同一源节点 `u` 的所有出边（包括共享后缀），计算每个边 `e` 的归一化正确性优势 `A_cor(e)`，通过组内标准化降低方差。
- **效率组（efficiency group）**：对于到达同一等价类 `Q` 的不同路径，计算路径长度 `ℓ_v` 和该类平均得分 `S̄_Q`，定义效率优势 `A_eff(v) = S̄_Q * ( -ℓ_v - mean(-ℓ_q) ) / std(-ℓ_q)`，均匀分配给路径上的每条边。
- 最终优势 `A_dual(e) = A_cor(e) + λ_eff * A_eff(e)`。

#### 策略优化
- 采用 PPO 风格目标，每个 token 使用相同的图优势，结合重要性采样和 KL 散度约束。
- 公式（8）给出了完整的损失函数，其中优势为 `A_dual(e)`。

## 3. 实验设计

### 使用的数据集与场景
- **推理基准**：AIME24、AIME25、MATH500、GPQA、LiveCodeBench。
- **智能体搜索任务**：General AI Assistant（GAIA）、WebWalkerQA、BrowseComp、XBench。

### Base 模型
- Qwen2.5-7B-Math
- Qwen3-8B-Base
- Deepseek-R1-Distill-Qwen-7B
- （智能体任务额外使用 Qwen2.5-7B-Instruct 配合 ReAct 框架）

### 对比方法（基线）
- **链式 RLVR**：GRPO、DAPO
- **树式推理 RL**：TreeRL、SPO、TREE-GRPO、PROS、TreePO
- 所有对比均在同等的 token 预算或轨迹预算下进行。

## 4. 资源与算力

- 论文明确说明：**实验在 16 块 H20 GPU 上进行**，使用 vLLM 进行推理，基于 Verl 框架（推理任务）和 Search-R1 框架（智能体任务）。
- 训练步数：推理任务 350 步，智能体任务 40 步；batch size 分别为 512（推理）和 128（智能体）；minibatch size 分别为 32 和 64。
- 文中未给出训练总时间或 GPU 小时数。

## 5. 实验数量与充分性

### 实验组数
- **主实验结果**：表 1 展示了 3 个模型 × 5 个推理基准 × 10 种方法（含 GraphPO）的对比（共计 150 个数据点），表 2 展示了 4 个智能体任务 × 4 种方法的对比。
- **消融实验**：
  - 合并阈值 `κ` 的影响（图 6）
  - 池化系数 `w` 的影响（图 S.2）
  - 不同分支策略对比（表 S.1）
  - 效率优势的影响（图 S.1）
  - PRM 方法对比（图 5a）
  - 相同轨迹预算对比（图 5b）
  - 轨迹生成效率对比（图 5c）
- **训练动态分析**：准确率、熵、响应长度随训练步数的变化（图 4）。
- **理论分析**：附录 F 提供了方差、探索效率、梯度方向、推理效率的理论证明。

### 充分性与公平性
- 实验覆盖多种模型（不同规模和预训练方法）、多种任务（数学推理 + 智能体搜索）、多种基线（链式、树式），并考虑了 token 预算和轨迹预算两种公平对比条件。
- 消融实验全面，验证了关键超参数的作用。
- 数据充分、对比客观、结论有理论支撑。

## 6. 论文的主要结论与发现

1. **GraphPO 在相同 token 预算下始终优于所有链式和树式基线**，在所有模型和基准上取得最高平均准确率。
2. **图结构降低了优势估计方差**：通过共享等价类后缀，增大了每组比较的样本量，理论证明方差低于树方法。
3. **提高了推理效率**：效率优势促使策略选择到达正确状态的更短路径，训练后响应长度明显缩短（图 4c）。
4. **维持了良好的探索多样性**：熵下降速度慢于树方法，说明图结构避免了对少数高奖励模式的过度放大。
5. **无需人工过程标注**：从结果奖励中自涌现出过程监督信号，且梯度方向与新奇门控的 Oracle PRM 一致（定理 3）。
6. **在智能体搜索任务中同样有效**，证明了方法的通用性。

## 7. 优点

- **方法创新性**：将推理轨迹建模为 DAG 并合并语义等价状态，突破了链式和树结构的局限，实现了跨路径的信息共享和预算重分配。
- **双组优势设计**：正确性组提供低方差步骤级信用分配，效率组鼓励推理简洁性，两者互补。
- **理论完整**：提供了方差降低、探索效率提升、梯度方向对齐、推理效率优化的正式证明。
- **实验充分且严格**：多种模型、任务、基线，以及消融和理论验证，结论可靠。
- **实用性强**：不依赖外部过程奖励模型，只需最终答案验证器即可获得过程监督，训练和推理效率高。

## 8. 不足与局限

- **实验领域覆盖有限**：论文主要聚焦数学推理和文本智能体搜索，未在多模态推理、代码执行反馈、长期交互游戏等更广泛场景验证。
- **依赖语义嵌入质量**：图构建依赖语义摘要和嵌入相似度，虽然理论表明对检测误差鲁棒（假阴性退化为树基线，假阳性由阈值控制），但实际中嵌入模型的偏差可能影响合并效果。
- **未报告完整训练耗时**：缺少总 GPU 时数或训练 wall-clock 时间，难以直接评估计算成本。
- **超参数敏感性**：合并阈值 `κ` 和池化系数 `w` 需适当调节，不同模型或任务可能需要单独调优（但文中实验显示在合理范围内性能稳定）。
- **未讨论长序列推理中的记忆限制**：DAG 节点数随推理步数增加可能增长，尽管通过合并减少了冗余，但极端长的推理任务中图管理开销仍需考量。

（完）
