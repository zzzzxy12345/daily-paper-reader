---
title: "Tensor-Coord: Algebraic Decomposition of Joint Plan Tensors for Conflict-Free Multi-Agent LLM Planning"
title_zh: Tensor-Coord：联合计划张量的代数分解用于无冲突多智能体LLM规划
authors: Mudit Rastogi
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16478v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于无冲突多智能体规划的代数张量分解
tldr: "多智能体LLM规划中独立生成计划常导致碰撞、资源冲突等协调失败。本文提出Tensor-Coord，将N个智能体的联合计划表示为三阶张量，利用CP和Tucker分解提取潜在协调结构，定义可计算的协调复杂度度量，并通过残差张量定位冲突。在2-4智能体网格任务中，2智能体100%在1.4轮内收敛到无冲突计划，3智能体80%在3.2轮内，4智能体60%在4.0轮内。该方法无需领域规则，可解释性强，为多智能体规划提供通用的冲突检测与修正框架。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16478v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1686, \"height\": 1156, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1589, \"height\": 1455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1565, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1702, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16478v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1755, \"height\": 318, \"label\": \"Table\"}]"
motivation: 多智能体LLM规划中独立生成计划会产生空间碰撞、资源争用和时间死锁等协调失败，亟需通用的冲突检测与纠正方法。
method: 将N个智能体的联合计划表示为三阶张量，使用CP和Tucker分解识别潜在协调结构，残差张量定位冲突并生成自然语言约束以迭代优化计划。
result: "在2/3/4智能体网格任务中，分别达成100%/80%/60%的无冲突计划收敛率，平均迭代次数1.4/3.2/4.0次。"
conclusion: 提出基于张量分解的可解释协调复杂度度量与冲突定位方法，显著提升多智能体LLM规划成功率，无需领域特定规则。
---

## 摘要
大语言模型在多智能体规划中仍然存在局限性，因为独立生成的规划可能导致协调失败，例如空间碰撞、资源争用和时间死锁。我们引入了Tensor-Coord，一个多线性代数框架，将N个智能体的联合计划表示为一个三阶张量T ∈ R^{N×H×A}，维度为智能体、时间步和动作。使用Canonical Polyadic (CP)和Tucker分解来识别潜在的协调结构。最小ε近似CP秩R*定义了一个可计算的协调复杂度度量CC(Pi) = (R* - N)/N。我们证明R*=N是计划独立性的必要且充分条件。残差E = T - T_{R*}定义了智能体对、时间步和动作上的冲突得分，无需领域特定规则即可定位失败。Tucker因子提供了可解释的智能体角色、时间阶段和动作聚类，这些被转化为自然语言约束，用于迭代LLM重新规划。在多机器人配送任务上的实验在简单（2智能体，5x5网格）、中等（3智能体，5x5网格）和困难（4智能体，5x5网格）设置中显示，在2智能体情况下100%收敛到无冲突计划，平均1.4次迭代；3智能体情况下80%在3.2次迭代内；4智能体情况下60%在4.0次迭代内。CP秩近似线性增长为R*(N) = 3.9N + 0.5，支持其作为协调复杂度预测器的使用。

## Abstract
Large language models (LLMs) remain limited in multi-agent planning because independently generated plans can create coordination failures such as spatial collisions, resource contention, and temporal deadlocks. We introduce Tensor-Coord, a multilinear algebra framework that represents the joint plan of N agents as a third-order tensor \(T \in R^{N \times H \times A}\) over agents, timesteps, and actions. Canonical Polyadic (CP) and Tucker decompositions are used to identify latent coordination structure. The minimal epsilon-approximate CP rank R* defines a computable coordination complexity measure, with \(CC(Pi)=(R*-N)/N\). We prove that R*=N is necessary and sufficient for plan independence. The residual \(E=T-T_{R*}\) defines a conflict score over agent pairs, timesteps, and actions, localizing failures without domain-specific rules. Tucker factors provide interpretable agent roles, temporal phases, and action clusters that are converted into natural language constraints for iterative LLM replanning. Experiments on multi-robot delivery tasks across Easy (2 agents, 5x5 grid), Medium (3 agents, 5x5 grid), and Hard (4 agents, 5x5 grid) settings show convergence to conflict-free plans in 100% of 2-agent cases within 1.4 iterations on average, 80% of 3-agent cases within 3.2 iterations, and 60% of 4-agent cases within 4.0 iterations. CP rank scaled approximately linearly as \(R*(N) = 3.9N + 0.5\), supporting its use as a predictor of coordination complexity.

---

## 论文详细总结（自动生成）

# Tensor-Coord 论文结构化总结

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：大语言模型（LLM）在多智能体规划中存在根本性缺陷——当多个智能体独立生成计划时，常导致空间碰撞、资源争用和时间死锁等协调失败。现有方法如联合提示（joint prompting）因动作空间指数增长超出上下文窗口、顺序规划存在顺序偏置、经典多智能体规划算法（如Conflict-Based Search）需要领域规范，无法兼具LLM的自然语言灵活性。
- **背景**：LLM本质上是概率性文本生成模型，而非确定性规划器；Valmeekam等（2024）证实在国际规划竞赛基准上GPT-4计划可执行性仅12.3%。多智能体扩展使联合状态空间和动作空间随智能体数指数增长，加剧了问题。
- **整体含义**：提出一种代数框架，将联合计划编码为三阶张量，利用张量分解自动发现协调结构，无需领域特定规则即可定位冲突并生成自然语言约束，从而实现无冲突的多智能体LLM规划。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：
  - 将N个智能体在T个时间步上选择K个动作的联合计划，编码为软张量 \( \mathcal{T} \in \mathbb{R}^{N \times T \times K} \)（使用标签平滑确保满秩）。
  - 通过Canonical Polyadic (CP)分解得到最小ε近似秩R*，定义协调复杂度度量 \( CC(\Pi) = (R^*-N)/N \)，证明R*=N是计划独立性的充要条件。
  - 从CP分解残差 \( E = \mathcal{T} - \mathcal{T}_{R^*} \) 计算冲突得分 \( CS(i,j,t) = \rho(i,t) \cdot \rho(j,t) \)，其中ρ为残差范数，用于代数地定位冲突。
  - 通过Tucker分解 \( \mathcal{T} \approx \mathcal{G} \times_1 U \times_2 V \times_3 W \) 提取可解释的智能体角色、时间阶段和动作类型，转化为自然语言约束。

- **关键技术细节**：
  - **软张量编码**：\( \mathcal{T}(i,t,k) = \gamma \cdot \mathbb{1}[a_i^t = a_k] + \frac{1-\gamma}{K-1} \cdot \mathbb{1}[a_i^t \neq a_k] \)，其中γ=0.8。
  - **CP分解**：使用交替最小二乘法（ALS）求解，从R=N向上递增直到重建误差<ε=0.1。
  - **Tucker分解**：通过高阶SVD（HOSVD）计算，对每个模态展开进行SVD。
  - **约束生成**：三类约束：空间约束（避免特定位置和时间）、阶段边界约束（同步）、角色约束（错时移动）。
  - **迭代算法**：在每次迭代中，LLM根据当前约束生成计划，Tensor-Coord检测冲突，生成新约束，循环最多K次。

- **算法流程**（Algorithm 1）：
  1. 初始化约束为空。
  2. 迭代（最多K次）：
     a. 每个智能体使用LLM生成计划（受约束）。
     b. 构建软张量。
     c. CP分解求R*，计算残差和冲突得分。
     d. Tucker分解提取结构。
     e. 若冲突数为0则返回；否则从冲突和Tucker因子生成自然语言约束，更新约束集。
  3. 返回最佳计划。

## 3. 实验设计

- **数据集/场景**：
  - 多机器人配送任务，网格世界。动作集A={北移、南移、东移、西移、拣货、卸货、等待}（K=7）。约束为空间排斥：同一时间不同机器人不能占据同一单元格。
  - 五个配置：
    - Easy: 2智能体, 5×5网格, 2包裹, T=10
    - Medium: 3智能体, 5×5网格, 3包裹, T=15
    - Hard: 4智能体, 5×5网格, 4包裹, T=20
    - Very Hard: 5智能体, 6×6网格, 5包裹, T=30
    - Complex: 3智能体, 6×6网格, 6包裹, T=35
- **Benchmark与对比方法**：
  - B1: 独立规划（无协调）
  - B2: 轮询规划（顺序认知先前计划）
  - B3: 单LLM联合规划（一次生成所有计划）
- **LLM后端**：Azure OpenAI GPT-5，温度0.2，最大令牌500。

## 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量、训练时长或总计算量**。仅提及使用Azure OpenAI GPT-5作为LLM后端，且实验规模较小（最多5智能体，35时间步），因此推测算力需求不大，但具体细节缺失。

## 5. 实验数量与充分性

- **实验数量**：
  - 每个配置使用5个随机种子，最大迭代次数5次。
  - 共5个配置 × 5种子 = 25组主要实验。
  - 对比了3个基线方法，在所有配置上都有对比结果（见图4和冲突缩减率）。
- **充分性与公平性**：
  - **优点**：覆盖了从简单到困难的多个难度级别；基线设置合理（零协调、顺序协调、单LLM联合），与Tensor-Coord使用相同LLM后端，确保了公平比较。
  - **不足**：
    - 未做消融实验（如去掉Tucker分解、只使用CP残差、改变γ值、不同阈值τ等）。
    - 仅报告了平均迭代次数和收敛率，未提供统计波动（标准差、置信区间）。
    - 只在网格配送任务上测试，领域单一，泛化性存疑（未在经典IPC规划基准、自然语言任务等上验证）。
    - 未与经典多智能体规划方法（如CBS）比较，虽然作者声称方法互补，但缺乏定量对比。

## 6. 主要结论与发现

- Tensor-Coord在2智能体（Easy）上100%收敛到无冲突计划，平均1.4次迭代；3智能体（Medium）80%收敛，平均3.2次；4智能体（Hard）60%收敛，平均4.0次。
- CP秩近似线性增长：R*(N) ≈ 3.9N + 0.5（R²=0.94），支持协调复杂度度量的有效性。
- 协调复杂度度量CC(Π)能可靠预测问题难度。
- 所有基线在冲突缩减率上均不及Tensor-Coord；独立规划在N>2时几乎完全失败；单LLM联合在N=3后表现下降。
- 发现“LLM回退悖论”：当LLM因约束过多返回等待计划时，意外降低了张量残差复杂度，加速收敛，提示显式等待协调是可行方向。

## 7. 优点（方法与实验亮点）

- **理论创新**：首次将CP秩与多智能体计划独立性建立形式化联系，提供可计算的协调复杂度度量，具有代数基础。
- **可解释性**：Tucker分解因子矩阵（角色、阶段、动作类型）直观可解释，转化为自然语言约束，符合人机协作需求。
- **无需领域规则**：冲突检测和定位完全基于张量残差，不依赖领域特定知识，通用性强。
- **迭代框架灵活**：可与任意LLM后端结合，约束生成可扩展。
- **实验设计**：问题规模从2到5智能体，且包含不同网格大小和包裹数，难度梯度清晰；基线设置合理，直接对比相同LLM后端。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅1个领域（网格配送），未在标准规划基准（如PDDL、Blocksworld）或真实机器人场景测试。
  - 未进行消融实验分析各组件贡献（如CP vs Tucker、γ影响、阈值τ敏感性）。
  - 未报告多次运行的标准差，统计稳定性不明确。
- **收敛率偏低**：4智能体Hard仅60%收敛，5智能体Very Hard仅20%，且剩余冲突数较高（10.4），说明当前约束生成策略在密集场景下不足。
- **资源与可复现性**：未提供代码或详细超参数，且未说明算力需求，其他研究者难以复现。
- **泛化风险**：仅在5×5和6×6网格上测试，更大网格、更多智能体或更复杂任务（如部分可观测、随机性）下的表现未知。
- **理论保证有限**：尽管有Theorem 1和Proposition 2，但未给出算法收敛率或最优性保证；迭代次数上限5次可能不足以解决非单调交互。
- **LLM依赖性**：方法依赖于LLM正确遵循自然语言约束，但LLM可能存在幻觉或忽略约束的风险，论文未深入分析这种失效模式。

（完）
