---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 面向长周期智能体强化学习的组图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:agent"]
score: 9.0
evidence: 面向长时域代理强化学习的组图信用分配
tldr: "长期智能体强化学习面临奖励稀疏和延迟问题，现有步级方法信用分配粗粒度且忽略状态图结构。本文提出G2PO，将线性轨迹转化为全局状态转移图，通过聚合相同观测降低状态值估计方差，并引入边中心优势估计全局标准化TD误差。在WebShop、ALFWorld和AppWorld上，G2PO相比GRPO等基线成功率最高提升22.2%，为长时域智能体任务提供了更有效的图结构信用分配方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有步级方法忽略状态转移的图结构，导致高方差状态值估计和局部信用分配，难以处理长期延迟奖励。
method: G2PO将交互轨迹转为全局状态图，聚合相同观测进行状态值估计，并提出边中心优势估计，全局标准化TD误差以识别关键转移。
result: "在WebShop、ALFWorld和AppWorld上，G2PO显著优于GRPO等基线，成功率最高提升22.2%。"
conclusion: G2PO通过图结构显式建模状态转移，实现更精准的信用分配，显著提升长期智能体任务性能。
---

## 摘要
基于组的强化学习（RL）显著增强了智能体场景下的大语言模型（LLM）。为实现更细粒度的策略更新，最近的智能体强化学习框架已从轨迹级训练转向步级训练。然而，长周期智能体强化学习面临严重的奖励稀疏性和延迟问题，因为反馈通常延迟数十个交互步骤。虽然现有的步级框架细化了训练粒度，但其信用分配仍然是粗粒度的，并且仍然将智能体探索视为孤立的线性轨迹。这种过度简化的视角忽略了状态转换的内在图结构，导致高方差的状态价值估计和短视的局部信用分配。为克服这些关键瓶颈，我们提出了组图策略优化（G2PO），这是一种专为多轮智能体任务设计的新型基于组的强化学习算法。G2PO明确地将线性交互轨迹转化为全局状态转换图。通过聚合不同轨迹中的相同观测，我们引入了组聚合状态价值估计，以降低采样方差和轨迹依赖偏差。此外，我们重新定义了智能体动作作为状态节点之间的转换，并提出了边缘中心优势估计策略。通过在全局范围内标准化整个图上的时间差分（TD）误差，G2PO明确识别并优先考虑推动绝对任务进展的关键转换。在代表性的长周期基准（WebShop、ALFWorld和AppWorld）上的大量实验表明，G2PO大幅优于最先进的基于提示和基于强化学习的基线，与GRPO相比，成功率提升了高达22.2%。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在长周期（long-horizon）智能体（Agent）强化学习场景中，大语言模型（LLM）需与环境进行多轮交互（如数十步），但最终奖励（terminal reward）仅在任务完成或超时后给出，导致严重的**奖励稀疏性和延迟**问题。虽然已有从轨迹级训练转向步级训练的方法（如 GiGPO），但现有步级方法仍将智能体探索视为**孤立的线性轨迹**，忽略了不同轨迹中观察到的相同/相似环境状态之间的内在联系（即状态转移图结构）。这种简化导致两个关键瓶颈：① **状态价值估计的高方差**（同一状态在不同轨迹中导致不同最终结果）；② **信用分配的局部化**（仅在同一状态下比较动作，无法识别全局关键突破性动作）。

- **整体含义**：论文旨在通过构建显式的**全局状态转移图**，实现更细粒度、更低方差、更全局化的信用分配，从而显著提升长周期智能体任务的性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将多条交互轨迹重构为一个**状态组图**（state group graph），以图视角进行价值估计和优势计算。

- **关键技术细节**：
  - **(1) 状态组图构建**：聚合所有轨迹中相同的观测（如相同网页内容）形成状态组节点，动作作为节点间的有向边，从而构建全局状态转移图。
  - **(2) 组聚合状态价值估计**：对每个状态组，其价值定义为组内所有步骤折扣回报的平均值（而非依赖单条轨迹的奖励）。这降低了采样方差，使价值估计更稳健（理论上证明当组大小>1时方差线性下降）。
  - **(3) 多粒度优势估计**：
    - **节点中心优势 (Node-Centric Advantage)**：在同一状态组内，对不同可选动作的后续状态价值进行局部归一化。
    - **边缘中心优势 (Edge-Centric Advantage)**：计算动作的1步TD误差（V(目标状态) - V(源状态)），并在全局范围内对所有边的TD误差进行标准化。该机制能识别绝对任务进展贡献大的关键动作。
    - **回合级优势 (Episode-Level Advantage)**：沿用GRPO的轨迹奖励全局标准化。
    - 最终优势 = 回合级优势 + w×(节点中心优势 + 边缘中心优势)。

- **算法流程**（文字说明）：
  1. 使用旧策略采样N条完整轨迹。
  2. 将所有观测聚类为状态组，构建图结构。
  3. 计算每个状态组的价值（组内所有步折扣回报的平均）。
  4. 计算每个动作的节点中心优势和边缘中心优势，再结合回合级优势得到最终优势。
  5. 使用带裁剪的代理目标函数（类似GRPO）和KL散度正则更新策略。

## 3. 实验设计：数据集/场景、Benchmark、对比方法

- **数据集/场景**：三个代表性的长周期智能体基准：
  - **WebShop**（电商场景，模拟购物网站），**ALFWorld**（具身物理环境，家庭任务），**AppWorld**（复杂软件生态系统，通过API调用完成任务）。
- **Benchmark指标**：成功率（Success Rate）和得分（Score）。
- **对比方法**：分为三类：
  - **前沿商用模型**：Qwen3.5-397B、DeepSeek-V3.2、Gemini-2.5-Pro。
  - **提示式方法**：直接提示、ReAct、Reflexion。
  - **RL训练方法**：PPO（带评论家）、RLOO、GRPO、GiGPO（最相关的步级基线）。
- **基础模型**：Qwen2.5-1.5B-Instruct、7B-Instruct、14B-Instruct（AppWorld因难度高用14B）。

## 4. 资源与算力

- **明确说明**：
  - ALFWorld和WebShop实验使用**4×H100 GPU**，训练100次迭代（iterations）。
  - AppWorld实验使用**8×H100 GPU**，训练50次迭代。
  - 所有组方法（包括G2PO）使用128个环境进行rollout（16组，每组8个环境）。
  - 学习率1×10⁻⁶，vLLM推理引擎。G2PO额外计算开销仅约1秒/训练步（CPU上执行图构建和优势计算），占总训练时间0.4%，几乎无额外成本。

## 5. 实验数量与充分性

- **实验数量**：
  - **主实验**：在3个基准上，对1.5B和7B模型（WebShop/ALFWorld）以及14B（AppWorld）进行了系统比较，每个结果平均3个随机种子，给出均值和标准差。
  - **消融实验**：在WebShop和ALFWorld上（Qwen2.5-1.5B），逐步增加组聚合、节点中心优势、边缘中心优势，验证各组件贡献。
  - **超参数分析**：针对平衡权重 w（0.6~1.2）在WebShop上的影响。
  - **方差分析**：统计状态价值估计的方差分布，证明组聚合的必要性。
  - **效率分析**：比较不同方法完成任务所需交互步数；单步训练时间分解。
  - **案例分析**：展示一个具体状态转移图，说明边缘中心优势的优势。
- **充分性评价**：实验设计较为充分，覆盖了多个维度的对比（不同模型大小、不同基准、不同基线种类），消融和超参数分析验证了核心设计，方差和效率分析提供了额外支持。但缺少在更多RL框架（如RAGEN）、更大模型（如70B）上的验证，且仅报告了三次运行的平均值，没有进行统计显著性检验（如t-test）。总体上，实验客观公平，基线用相同超参数和提示模板。

## 6. 论文的主要结论与发现

- G2PO在所有三个基准上**一致且显著优于**所有基线，包括GRPO、GiGPO等最先进的RL方法。在WebShop和ALFWorld上，相比GRPO成功率提升分别高达**22.2%和14.4%**（1.5B模型），在7B模型上也有相似幅度的提升。
- 组聚合状态价值估计有效降低了方差，是性能提升的关键；边缘中心优势提供了全局视角，优于仅靠局部比较的节点中心优势。
- G2PO能以**更少的交互步数**完成任务（效率更高），同时计算开销几乎为零（仅额外1秒/步）。
- 在复杂观察的AppWorld上，G2PO同样显示出有效性，验证了跨场景泛化能力。

## 7. 优点

- **方法创新**：首次将状态转移图显式引入组RL，解决了步级RL的方差和局部性问题。
- **实用高效**：几乎不增加计算成本（所有图操作在CPU上完成），可轻松集成到现有GRPO框架。
- **信用分配精细**：边缘中心优势从全局识别关键动作，避免“好动作因后续错误被惩罚”等问题。
- **理论支撑**：附录提供了方差减少的数学证明，增强了可信度。
- **实验全面**：覆盖多个维度，结果稳定，代码已开源。

## 8. 不足与局限

- **框架限制**：论文仅在verl-agent框架上实现，未在其他流行智能体RL框架（如RAGEN、AgentRL）上验证通用性。
- **采样方式**：当前使用线性rollout，未探索树采样、异步采样等更高效的采样策略（作者在局限中提及此方向）。
- **可扩展性**：当状态数量极大（如开放世界）时，图构建和聚类可能面临可扩展性挑战；论文未讨论此问题。
- **状态分组**：基于完全相同的观测进行分组，对于语义相似但文本不同的状态（如不同表述的相同状态），未考虑聚类宽松策略，可能限制收益。
- **统计显著性**：未报告p值或置信区间，仅凭标准差说明稳定性，说服力略弱。
- **奖励设计**：采用简单的规则奖励（成功=10，失败=0，无效=-0.1），未探索更细粒度的环境奖励，可能影响结果的泛化性。

（完）
