---
title: "Beyond the Next Step: Variable-Length Latent World Models for Long-Horizon Planning"
title_zh: 超越下一步：用于长视界规划的可变长度潜空间世界模型
authors: "Tianqi Du, Qi Zhang, Yifei Wang, Yisen Wang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21775v1"
tags: ["query:agent"]
score: 9.0
evidence: 面向智能体长时程规划的世界模型
tldr: "现有潜在世界模型通常仅进行单步预测，递归展开实现长时域规划，导致误差累积与训练-推理不一致。为克服此局限，本文提出变长潜在世界模型VLWMs，直接预测变长动作序列对应的潜在状态，不再依赖递归迭代，并采用课程训练逐步扩展预测长度。同时，为VLWMs设计了专门的规划方法以充分利用其变长预测能力。在长时域控制任务上，VLWMs相比现有最先进的LeWM取得了平均13%的性能提升，尤其擅长需要长规划的任务。此工作为潜在世界模型在长时域预测与规划方面提供了一种简单而有效的范式，具有广泛的应用前景。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21775v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1350, \"height\": 807, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1329, \"height\": 678, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21775v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1161, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1019, \"height\": 224, \"label\": \"Table\"}]"
motivation: 克服现有潜在世界模型依赖单步预测递归展开导致的误差累积和训练-规划不匹配问题。
method: 提出VLWMs，直接预测变长动作序列对应的未来潜在状态，并采用课程训练策略逐步扩展预测视野，同时设计适配规划方法。
result: "在长时域控制任务上，VLWMs相比LeWM平均提升13%，尤其在需长规划的任务上增益显著。"
conclusion: VLWMs为潜在世界模型的长时域预测与规划提供了简单有效的改进范式，具有广泛应用价值。
---

## 摘要
最近，世界模型通过学习预测模型来构建智能体，该模型基于观测和动作估计未来环境状态，已成为一个有前景的范式。特别是JEPA风格的潜空间世界模型通过在紧凑表示空间中学习基于动作的动力学，提供了像素空间预测的有效替代方案。然而，现有的潜空间世界模型通常依赖单步预测，必须递归展开以进行长视界规划，这会导致误差累积以及训练目标与下游规划任务之间的不匹配。为了解决这一局限，我们提出可变长度潜空间世界模型（VLWMs），该框架学习基于可变长度动作序列预测未来潜状态。与仅在单步转移上训练不同，VLWMs直接建模时间扩展的动力学，使得同一预测器能够评估不同时域的动作计划。我们进一步引入了一种课程训练策略，逐步扩展动作时域，稳定从短程动力学到长程预测的优化。在测试时，我们设计了针对VLWMs的规划方法，以更好地利用其可变长度预测能力。在长视界控制任务上的实验表明，VLWMs显著改进了潜空间世界模型，在不同数据集上相比最先进的LeWM平均提升13%，尤其是在需要扩展规划的任务上提升巨大。这些结果表明，VLWM为改进潜空间世界模型中的长视界预测与规划提供了一种简单而有效的范式。

## Abstract
Recently, world models have emerged as a promising paradigm for building intelligent agents by learning predictive models that estimate future environment states conditioned on observations and actions. In particular, JEPA-style latent world models provide an efficient alternative to pixel space prediction by learning action-conditioned dynamics in compact representation spaces. However, existing latent world models typically rely on one-step prediction and must be recursively rolled out for long-horizon planning, which leads to compounding errors and a mismatch between training objectives and downstream planning tasks. To address this limitation, we propose Variable-length Latent World Models (VLWMs), a framework that learns to predict future latent states conditioned on action sequences of variable lengths. Instead of training only on one-step transitions, VLWMs directly model temporally extended dynamics, allowing the same predictor to evaluate action plans over different horizons. We further introduce a curriculum training strategy that progressively expands the action horizon, stabilizing optimization from short-range dynamics to long-range prediction. At test time, we design planning methods tailored to VLWMs to better exploit their variable-length predictive capabilities. Experiments on long-horizon control tasks show that VLWMs significantly improve latent space world models, achieving 13\% average improvement over the state-of-the-art LeWM across different datasets, with especially large gains on tasks requiring extended planning. These results suggest that VLWM provides a simple yet effective paradigm for improving long-horizon prediction and planning in latent world models.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：潜空间世界模型（如JEPA、DINO-WM、LeWM）通过紧凑表示空间学习动作条件动力学，避免了像素级预测的计算开销。然而，现有模型普遍采用**固定单步预测**（t→t+1），在长视界规划时需递归展开，导致误差累积（compounding error），且训练目标（局部单步）与规划任务（长时域）不匹配。
- **核心挑战**：视频帧间存在高时间冗余，单步预测易退化为短期插值，无法捕捉高层语义动力学；递归展开进一步放大误差，长视界规划质量急剧下降。
- **论文目标**：提出可变长度潜空间世界模型（VLWM），直接建模从t到t+k（k可变）的扩展动力学，统一短程与长程预测，从根本上缓解误差累积和训练-规划错配。

## 2. 方法论
### 核心思想
- 训练单一预测器 \( g_\phi \)，输入当前潜状态 \( s_t \) 和可变长度动作序列 \( a_{t:t+k-1} \)，直接输出未来潜状态 \( \hat{s}_{t+k} \)。
- 监督信号为目标编码器（EMA副本）对 \( t+k \) 时刻观测的潜表示（stop-gradient），损失为L2距离：
  \[
  \mathcal{L}_{\text{VLWM}} = \mathbb{E}_{t, k \sim p(k)} \left\| g_\phi(s_t, a_{t:t+k-1}) - \text{sg}(\bar{f}_{\bar{\theta}}(o_{t+k})) \right\|_2^2
  \]
  其中 \( p(k) \) 为课程训练中的均匀分布。

### 关键技术细节
- **动作作为Token（Action-as-Token）**：将每个动作嵌入视为标准Transformer token，与状态token交错排列在输入序列中：`[s_{t-L+1}, ..., s_t, e_t, e_{t+1}, ..., e_{t+k-1}]`。因果Transformer直接读最后token输出作为预测 \( \hat{s}_{t+k} \)，无需额外预测头或掩码。状态/动作通过可学习类型嵌入区分，位置嵌入共享。
- **课程训练策略**：训练过程分为 \( K_{\text{max}} \) 个等长阶段，第j阶段均匀采样 \( k \in \{1,...,j\} \)。初始仅学k=1，逐步扩展至最大时域 \( K_{\text{max}} \)。该策略避免直接长时域训练的不稳定性，同时保留短程动力学。
- **规划方法（三种变体）**：将规划时域H分解为多个块（chunks）\( k_1,k_2,...,k_M \)，每个块直接跳过多步。三种变体：
  - **P1（固定块大小）**：所有块使用相同 \( k^\star \)，可任务自适应选择。
  - **P2（随机块大小）**：均匀采样 \( k_i \in \{1,...,K_{\text{max}}\} \)，约束总和为H。
  - **P3（长到短）**：根据位置依赖分布，前期用大块快速覆盖长程结构，后期用小块精细调整。
  三种规划器共享相同VLWM权重，仅改变块调度，体现了变长训练作为更灵活规划接口的优势。

## 3. 实验设计
### 数据集/场景
- **PushT**：2D连续推动任务，蓝色圆点推动T形块至目标构型。20,000条专家轨迹，平均长度196步。
- **OGBench-Cube**：3D机器人操作，机械臂抓取立方体并放置至目标位置。10,000条轨迹，每步200帧。
- **TwoRoom**：2D导航，红色圆点从一间房穿越门到另一间房。10,000条轨迹，平均长度92步。

### Benchmark与对比方法
- 基准方法：**LeWM**（LeWorldModel），当前最先进的潜空间世界模型。
- 所有方法使用相同编码器（ViT-Tiny）、预测器架构、优化器、CEM规划器参数，确保公平对比。
- 评估协议：目标条件规划，目标偏移 \( \Delta \in \{25,50,75,100\} \)，评价预算为50环境步骤。成功率按环境特定标准判定（如块配置误差、与目标距离等）。

### 对比的消融/变体
- 三种VLWM规划策略（P1, P2, P3）相互比较。
- 课程训练 vs 无课程训练（直接训练全范围 \( \{1,...,K_{\text{max}}\} \)）。
- 线性探针评估：在PushT上冻结编码器，训练线性层预测物理属性（智能体位置、块位置、块角度），比较MSE和Pearson相关系数。

## 4. 资源与算力
- 论文在实现细节中未明确说明使用的GPU型号、数量及训练总时长。仅提及：
  - 训练使用AdamW优化器（lr=5e-5, weight decay=1e-3），batch size=128，bfloat16混合精度。
  - 最多训练100个epoch，但在第10个epoch评估（足够达到最佳规划性能）。
  - 帧跳跃(frame skip)=5。
- **结论**：文中未说明具体硬件配置与训练耗时，属于信息缺失。

## 5. 实验数量与充分性
### 实验组别
- 主实验（图2）：在三个环境上，每个环境4个Δ值，VLWM（oracle规划器） vs LeWM，共12个数据点。
- 规划策略对比（表1）：P1（k=3,5）、P2（Kmax=5）、P3（Kmax=3,5）共5种变体×4Δ×3环境 = 60个结果。
- 课程训练消融（表2）：with/without curriculum ×4Δ×3环境 = 24个结果。
- 线性探针评估（表3）：VLWM vs LeWM在3个物理属性上的MSE和r值。
- 定性可视化（图3）：TwoRoom上滚动轨迹对比。

### 充分性评价
- **覆盖范围**：涵盖了2D/3D、导航/操作等不同类型任务，以及不同时域长度，较好验证了通用性。
- **公平性**：与LeWM保持相同骨干网络、优化器、规划器超参数，仅修改训练目标和动作编码方式，归因明确。
- **消融完整**：对课程训练、规划策略进行分组消融，并额外用线性探针评估表示质量，实验设计较全面。
- **局限性**：未与其他变长世界模型（如HWM）进行直接实验对比（仅在相关工作论述区别）；未在离散动作空间或更复杂视觉环境（如真实机器人）验证；未给出统计显著性或多次随机试验结果。

## 6. 主要结论与发现
- VLWM在所有三个环境上一致超越LeWM，尤其在长视界（Δ=75,100）上增益显著。例如TwoRoom-100成功率从36%提升至68%，TwoRoom-75从34%至70%，OGBench-Cube-100从42%至50%。
- **规划策略选择**：不同环境/时域偏好不同策略——P2（随机）在OGBench-Cube上最佳，P3（长到短）在PushT和TwoRoom长视界上最佳。无单一规划器全局最优，体现变长规划接口的丰富性。
- **课程训练**：在PushT和TwoRoom上有明显提升（尤其长视界），在OGBench-Cube上增益有限，可能因该任务长时域并不显著更难。
- **表示质量**：VLWM的潜表示在线性探针中具备更高的Pearson相关系数，尤其在块角度属性上，说明变长监督鼓励编码器捕捉更丰富的语义结构。

## 7. 优点
- **核心创新简洁有效**：直接修正单步预测的根本缺陷，而非增加复杂的层级或外部模块，方法易于实现。
- **动作作为Token设计灵活**：消除了固定动作条件接口约束，使模型天然支持任意长度动作序列，且与现有编码器架构解耦。
- **课程训练稳定优化**：逐步扩展预测时域避免了长时域训练的不稳定性，同时保留短程动力学。
- **统一规划接口**：单一模型支持多种变长规划策略（固定、随机、长到短），实现了层次化规划无需单独的高层策略。
- **实验设计公平充分**：与强基准LeWM保持相同骨干和优化设置，消融实验清晰，线性探针提供了额外表示质量证据。

## 8. 不足与局限
- **实验覆盖不全**：未与同类变长世界模型HWM进行直接实验比较，仅文本论述区别；未在离散动作空间或真实机器人场景验证。
- **课程训练在部分任务上无效**：在OGBench-Cube上课程训练几乎没有提升甚至略降，表明其效果依赖任务特性。
- **规划策略选择无自动化规则**：不同任务/时域需手动挑选最佳规划器（P1/P2/P3），缺乏自适应选择机制。
- **算力信息缺失**：未报告GPU型号、数量、训练时长，不利于复现和评估实际资源需求。
- **潜表示解码仅定性**：图3的可视化通过解码潜表示为像素，但未给出定量重构指标（如PSNR或FID），可能掩盖表示保真度细节。
- **应用限制**：方法依赖离线数据集，对分布外场景的泛化能力未验证；最大预测时域Kmax=5（对应25环境步），更长时域下的伸缩性未探索。

（完）
