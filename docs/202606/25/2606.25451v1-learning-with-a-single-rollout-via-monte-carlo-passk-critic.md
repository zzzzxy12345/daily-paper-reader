---
title: "Learning with a Single Rollout via Monte Carlo Pass@k Critic"
title_zh: "基于单次Rollout的蒙特卡洛Pass@k评判器学习"
authors: "Fengdi Che, Yang Liu, Lei Yu, Meng Cao, Tong Che, Rupam Mahmood, Dale Schuurmans"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25451v1"
tags: ["query:agent"]
score: 9.0
evidence: 单次rollout的PPO及令牌级信用分配
tldr: "语言模型强化学习中，重复采样生成轨迹导致计算昂贵且信用分配困难。本文提出单次rollout的PPO（SR-PPO），训练一个基于蒙特卡洛Pass@k的token级评论家，预测前缀的成功概率。随着k增大，该指标趋近于可达性指示器，无需对比采样即可进行信用分配。在数学推理任务上，SR-PPO稳定提升了Pass@128成功率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25451v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1455, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1428, \"height\": 350, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1366, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1376, \"height\": 901, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25451v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1467, \"height\": 222, \"label\": \"Table\"}]"
motivation: 现有RL方法需要重复采样导致计算成本高，且结果奖励稀疏难以分配给中间步骤。
method: "只进行一次rollout，训练一个预测Pass@k成功概率的token级评论家，k大时可达性可作为直接信用分配。"
result: "在HMMT26和AIME24上，SR-PPO比GRPO等取得更高的Pass@128成功率，且学习动态稳定。"
conclusion: "单次rollout结合Pass@k评论家有效缓解了重复采样和信用分配难题。"
---

## 摘要
在语言模型的强化学习（RL）中，估计token级别的优势仍然具有挑战性，因为扩展情节经验收集的成本很高。对于基线优势估计方法，难度进一步加大：重复采样会导致轨迹发散到显著不同的推理前缀。在此背景下，诸如GRPO之类的RL算法显得有限：结果奖励过于稀疏，难以归因于中间步骤等特定动作，并且跨采样轨迹的比较并非易事，因为它们具有异质性。为了缓解重复采样的计算成本和信用分配的困难，我们研究了面向语言模型RL中具有token级信用分配的单rollout近端策略优化（SR-PPO）。我们不通过在候选组内归一化情节回报来估计优势，而是使用每个提示词的一次rollout中的蒙特卡洛结果来训练校准的token级信用评判器。具体而言，我们使用评判器来预测提示前缀的Pass@k成功概率，该概率源自一次Pass@1尝试。这个选择比Pass@1提供了更具选择性的学习信号：它降低了易于解决的前缀的权重，同时优先考虑那些成功概率仍然很小的困难前缀。我们证明，随着k增大，Pass@k收敛到一个可达性指标，反映了某个前缀是否能导致至少一个成功延续。在显式状态图中，极限（k → ∞）可以在O(|V|+|E|)时间内计算，为无需采样对比轨迹的直接信用分配提供了一个有前途的替代方案。作为初步验证，SR-PPO展现出稳定的学习动态，并在HMMT26和AIME24等数学推理基准上持续提升Pass@128成功率。

## Abstract
Estimating token-level advantages in reinforcement learning (RL) for language models remains challenging because scaling up episodic experience collection is expensive. The difficulty intensifies for baseline advantage estimation methods, where repeated sampling causes trajectories to diverge into substantially different reasoning prefixes. In this context, RL algorithms such as GRPO prove limited: an outcome reward is too sparse to be attributed to specific actions like intermediate steps, and comparisons across sampled traces are non-trivial because they are heterogeneous. To mitigate both the computational cost of repeated sampling and the difficulty of credit assignment, we study single-rollout proximal policy optimization (SR-PPO) featuring token-level credit assignment in RL for language models. Instead of estimating advantages by normalizing episodic returns within the candidate group, we train a calibrated token-level credit critic using Monte Carlo outcomes from one rollout per prompt. Specifically, we use the critic to predict the Pass@k success probability at the prompt prefix, which is derived from a Pass@1 attempt. This choice yields a more selective learning signal than Pass@1: it discounts easily solved prefixes while prioritizing hard ones whose success probability remains marginal. We show that as $k$ increases, Pass@k converges to a reachability indicator, reflecting whether a prefix can lead to at least one successful continuation. In an explicit state graph, the limit ($k \rightarrow \infty$) can be computed in $O(|V|+|E|)$ time, offering a promising surrogate for direct credit assignment without the need to sample contrastive traces. As an initial validation, SR-PPO exhibits stable learning dynamics, along with consistent gains in Pass@128 success rates on mathematical reasoning benchmarks such as HMMT26 and AIME24.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在语言模型（LM）的强化学习（RL）中，传统的基于多rollout（如GRPO）的方法计算成本高，且轨迹异质性导致跨轨迹信用分配困难。同时，结果奖励（outcome reward）稀疏，难以归因到中间步骤（前缀），引发“前缀陷阱”问题。
- **背景**：在数学推理、代码生成等可验证奖励领域，RL后训练很常见。但重复采样对于长程agent系统（如多步推理、工具调用）代价极大。因此，需要一种**单次rollout**的信用分配机制，仅利用每提示的一次轨迹就能提取有效的令牌级训练信号。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出SR-PPO（Single-Rollout Proximal Policy Optimization）。训练一个**前缀级信用评判器**（credit critic），预测从当前前缀出发获得最终正确答案的概率（Pass@k）。通过比较相邻前缀的预测值，得到令牌级优势估计，进而更新策略。关键选择是使用**Pass@k**而非Pass@1：k较大时，信号更具选择性，强调困难但可达的前缀，减轻已在简单前缀上的无效更新。
- **关键技术细节**：
  - **前缀信用模型**：用蒙特卡洛结果监督训练评判器，损失函数包含BCE和Brier评分项，提高概率校准。
  - **Pass@k变换**：前缀值 `q_π(s) = Pass@1` 是其成功概率。定义 `q_{π,k}(s) = 1 - (1 - q_π(s))^k`，表示至少一次成功概率。训练时网络直接预测 `q_{π,k}`，再逆变换回 `q_π` 用于损失。
  - **令牌级优势**：`A_t = (ˆq(s_t) - ˆq(s_{t-1})) + λ(Y - ˆq(s_T))`，其中第一项为局部变化，第二项为终端校准。λ=1。
  - **策略梯度目标**：单步on-policy更新，等值于密度策略梯度（因每个批次使用新采样，clip失效）。
  - **理论连接**：当k→∞时，Pass@k收敛到二值可达性（reachability），即前缀是否能导向成功。若有显式状态图，可达性可在O(|V|+|E|)时间计算，为无需采样的直接信用分配提供可能。

## 3. 实验设计：数据集、基准和方法对比
- **数据集**：
  - 训练：**DeepScaleR**数学推理数据集。
  - 评估：**AIME24**、**AIME25**、**HMMT26**（竞争级数学基准）。
- **基准方法**：
  - **GRPO**（Group Relative Policy Optimization）：每组8个rollout，每组128 prompts。
  - **GAE-PPO**：使用广义优势估计和TD引导的标准PPO，单rollout。
  - SR-PPO的对比变体：Pass@1 SR-PPO、Pass@4 SR-PPO、不同梯度累积步数（g=1,2,4）。
- **评价指标**：Pass@k成功率（评估时采样128次，估计Pass@k for k<128）。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。
- 但从描述可推断：基模型为Qwen3-1.7B（约1.7B参数），最大响应长度9182 tokens。训练批量大小为256 prompts（SR-PPO），GRPO为128 prompts×8 rollouts=1024 rollouts/步。梯度累积步数影响更新频率。整体属于中等规模实验。

## 5. 实验数量与充分性
- **主要实验**：图1(b)比较GRPO、SR-PPO、GAE-PPO的验证Pass@8成功率随训练步数变化；图3在三个基准（AIME24,25,HMMT26）上比较GRPO和Pass@4 SR-PPO在推理时不同采样数下的表现；图1(c)比较Pass@4和Pass@1 SR-PPO在相同rollout预算下的稳定性。
- **消融实验**：表1展示Pass@1与Pass@4的令牌级优势稀疏度对比（百分比）。还提供了定性案例分析（附录C的令牌优势可视化）。
- **充分性与客观性**：实验设计较为规范，对比了多个基线方法，考虑了不同k值和梯度累积。但局限在于：仅使用单一种子（single seed）、单一基模型（Qwen3-1.7B）、单一训练数据集（DeepScaleR）。未在多领域、多种模型规模（如7B/70B）或长程agent环境中验证，因此泛化性有限。

## 6. 论文的主要结论与发现
- SR-PPO（Pass@4）虽然训练时每提示仅用一个rollout，但能达到与使用8个rollout的GRPO相当的Pass@8验证性能，表明前缀信用模型能有效提取稀疏监督中的信号。
- 标准GAE-PPO在单rollout regime下几乎无法学习（格式分数甚至不提升），凸显长期数学推理中TD方法的不适用性。
- Pass@k（尤其Pass@4）产生更稀疏、更稳定的令牌级优势，减少了简单前缀上的无效更新，表现为表1中近零优势令牌比例高。
- Pass@1 SR-PPO变体不稳定，尤其梯度累积多步时性能退化。
- 推理时采样数增加，SR-PPO训练后的模型性能线性提升，表明训练不仅提升贪婪解，也改善了采样分布质量。

## 7. 优点
- **计算效率**：单rollout显著减少采样计算和存储开销，适合长程agent等昂贵场景。
- **Credit分配机制**：通过训练前缀级预测器，将稀疏二元结果转换为密集的令牌级学习信号，无需多轨迹对比。
- **Pass@k的选择性**：可自动降低易解前缀权重、聚焦困难前缀，提高训练稳定性。
- **理论联系**：将Pass@k与可达性统一，给出了图论下的高效算法，为未来非采样信用分配提供方向。
- **实验验证**：在多个数学竞赛基准上展示性能提升，并揭示Pass@k vs Pass@1的稳定性和稀疏性权衡。

## 8. 不足与局限
- **实验范围有限**：仅用单一种子、单一模型（Qwen3-1.7B）、单一训练数据集（DeepScaleR），结论的泛化性未知。
- **未验证更长/更复杂场景**：论文提及适用于agent系统（工具调用、多跳检索），但实验仅在短文本数学推理上，未在长程agent任务（如软件工程、GUI操作）上验证。
- **对超参数敏感**：SR-PPO需要调优学习率、KL系数、评判器损失权重等，论文未进行大规模超参搜索，最优设置可能随模型/任务变化。
- **Pass@k值的逆变换依赖校准**：若评判器预测不准，逆变换的Pass@1可能不可靠。论文虽用Brier评分校准，但未深入分析校准误差影响。
- **无大规模算力实验**：未报告训练时长、GPU型号/数量，难以复现和比较资源消耗。

（完）
