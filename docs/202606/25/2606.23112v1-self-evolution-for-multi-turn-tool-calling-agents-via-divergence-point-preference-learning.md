---
title: Self-Evolution for Multi-Turn Tool-Calling Agents via Divergence-Point Preference Learning
title_zh: 基于分歧点偏好学习的多轮工具调用智能体的自我进化
authors: Jiaqiang Tang
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23112v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于分歧点偏好学习的多轮工具调用智能体自我进化
tldr: "多轮工具调用代理需协调长序列工具并跟踪状态，现有方法分离推理与参数学习导致结构弱化。本文提出ToolGraph，利用模式拓扑、转移权重和历史感知控制构建结构化上下文，并通过分歧点偏好学习从自生成轨迹中构建161个偏好对，用DPO微调。在tau2-bench的375个任务上，ToolGraph将加权平均奖励从0.304提升至0.338（+11.2%），结合DPO达到0.355（+16.8%），增益集中于航空和零售领域。该方法展示了自进化范式在复杂工具调用任务中的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23112v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1624, \"height\": 937, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23112v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1437, \"height\": 631, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23112v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 851, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23112v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 707, \"height\": 419, \"label\": \"Table\"}]"
motivation: 现有方法分离推理时工具选择与参数学习，导致长序列工具协调结构弱且偏好更新受提示不匹配影响。
method: 提出ToolGraph结合模式拓扑与转移权重，并基于状态匹配构建161个分歧点偏好对，在相同上下文下用DPO训练。
result: "在tau2-bench上，ToolGraph+DPO奖励达0.355，相对基线提升16.8%，航空零售领域增益最显著。"
conclusion: 自进化方法有效提升多轮工具调用性能，但电信任务常耗尽步骤预算；选择奖励正性是最优检查点信号。
---

## 摘要
多轮工具使用智能体必须协调长期工具序列，同时跟踪对话状态和策略约束。现有方法通常将推理时的编排与参数级学习分离，导致工具选择结构松散，且偏好更新容易受到训练与部署提示不匹配的影响。为实现基准内的自我改进，ToolGraph结合了从模式导出的拓扑结构、从成功轨迹中估计的转移权重，以及针对写入前提和重复搜索循环的历史感知控制。我们通过基于状态匹配和基于前缀对齐的方式定位分歧点，构建了161个偏好对，并通过动作正确性标注进行过滤，在推理时使用的同一ToolGraph上下文中训练DPO。在375个tau2-bench任务上，ToolGraph将加权平均奖励从0.304提升至0.338（相对提升11.2%），而ToolGraph+DPO达到0.355（相对基线提升16.8%），DPO的提升主要集中在航空和零售领域。细粒度诊断进一步显示，大约一半的电信轨迹在动作执行前耗尽步骤预算，并且在我们评估的16个DPO配置中，所选奖励的正向性是最有用的检查点信号。

## Abstract
Multi-turn tool-using agents must coordinate long-horizon tool sequences while tracking dialogue state and policy constraints. Existing approaches often separate inference-time orchestration from parameter-level learning, leaving tool selection weakly structured and preference updates vulnerable to train--deployment prompt mismatch. For within-benchmark self-improvement, ToolGraph combines schema-derived topology, transition weights estimated from successful rollouts, and history-aware controls for write prerequisites and repeated-search loops. We then construct 161 preference pairs by locating divergence points via state-based matching and prefix-based alignment, filtered through action-correctness annotations, and train DPO under the same ToolGraph context used at inference. Across 375 tau2-bench tasks, ToolGraph raises the weighted average reward from 0.304 to 0.338 (+11.2% relative), while ToolGraph+DPO reaches 0.355 (+16.8% over the baseline), with the DPO gain concentrated in airline and retail. Fine-grained diagnostics further show that roughly half of telecom trajectories exhaust the step budget before action execution and that chosen reward positivity is the most useful checkpoint signal across our 16 evaluated DPO configurations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多轮工具调用智能体需要协调长序列工具调用，同时跟踪对话状态和策略约束。现有方法将推理时的工具编排与参数级学习分离，导致工具选择结构松散，且偏好更新易受训练与部署提示不匹配的影响。
- **研究背景**：tau2-bench 基准测试涵盖了航空、零售、电信、银行四个领域的客服场景，要求智能体使用领域工具集、跟踪对话状态、遵循策略约束，并在适当时候停止信息收集并执行动作。部署本地模型（Qwen/Qwen3.5-9B）需要可靠的工具选择和自我进化循环。
- **整体含义**：论文旨在探索能否将智能体自身的基准测试经验同时用作结构化推理指导和与提示对齐的偏好监督，从而实现基准内的自我改进。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：通过两个协调组件实现自我进化：
  1. **ToolGraph 编排**：将工具模式与成功轨迹转换为结构化的图形指导，用于推理时的工具选择。
  2. **DPO 自我进化**：在推理时使用的同一 ToolGraph 上下文中，从智能体自身的成功/失败轨迹构建分歧点偏好对，并训练 DPO。
- **关键技术细节**：
  - **ToolGraph 构建**：
    - 节点：每个域的工具（名称、描述、参数）。
    - 边：基于共享参数分析构建，并对操作（act）工具添加回退边。
    - 边权重：从 532 条奖励为 1.0 的成功轨迹中计算工具成功率和转移概率，并进行每任务降权。
    - 规划提示：包含执行阶段（identify → gather → act）、并行集、关键规则（如航空：取消/修改前先调用 get_reservation_details）。
    - 束搜索与最佳 N 选择：生成最多三个候选，应用五种调整：重复惩罚、边奖励、前动作门控、回溯奖励/惩罚、电信循环检测。
  - **DPO 自我进化**：
    - 故障归因：将低奖励轨迹分为 redundant read、progress stall、tool execution error、max-steps exceeded、strategy error 五类。
    - 偏好对构建：通过状态匹配（第一阶段）或前缀对齐（第二阶段）定位分歧点，确保选择动作来自成功轨迹，拒绝动作来自失败轨迹，并通过动作正确性标注过滤。
    - 训练：4-bit NF4 QLoRA，LoRA rank=16，α=32，dropout=0.05，针对 Qwen 的 attention 和 MLP 投影。训练超参数包括 β（0.05-0.20）、学习率（2e-6、5e-6、8e-6）、训练步数（60-200）。使用标准 DPO 损失。
    - 上下文一致性：训练时注入与推理相同的 ToolGraph 规划提示，避免 v1 中的提示不匹配问题。
- **算法流程**（文字说明）：Algorithm 1 展示了偏好对构建流程：先提取故障轨迹的状态签名，在成功轨迹中搜索兼容状态；若找到且动作不同，则构造对；否则回退到前缀对齐（LCP50 定位共享前缀），进一步检查动作正确性，最终生成 (x, y+, y-) 对。

### 3. 实验设计：数据集/场景、基准、对比方法
- **数据集/场景**：tau2-bench 基准测试的四个领域，共 375 个任务：航空（50）、零售（114）、电信（114）、银行（97）。
- **基准**：tau2-bench 自身，评估最终状态奖励。
- **对比方法**：
  - Baseline：无任何 ToolGraph 干预的原始模型。
  - ToolGraph（TG）：使用加权 ToolGraph 编排。
  - TG + DPO R2#4：TG 基础上叠加 DPO 微调。
- **评估指标**：加权平均奖励（reward），以及每领域平均奖励、错误率（MAX_STEPS、低奖励等）、诊断子集（DB match、action all-OK）。

### 4. 资源与算力
- 论文提到使用 **HPC2 A800 GPUs** 通过 vLLM 部署模型，但未明确指明 GPU 的具体型号、数量和训练时长。仅提及“single baseline run”和“evaluate on the same HPC2 A800 GPUs”。对于 DPO 训练，使用了 QLoRA 微调，但未说明具体的 GPU 小时数或节点数。因此，资源细节并不透明。

### 5. 实验数量与充分性
- **实验数量**：主要三组对比（Baseline、TG、TG+DPO），DPO 部分评估了 **16 个配置**（β、学习率、步数的组合，非全因子网格）。每领域单独报告结果。
- **充分性**：实验覆盖了四个领域共 375 个任务，但仅有“single baseline run”（没有报告多次运行的标准差或置信区间）。ToolGraph 机制被联合评估，没有进行组件消融（如分别测试 edge weights、pre-act gate、loop detection 的独立贡献）。DPO 配置搜索较为有限（16个配置），但提供了有代表性的结果。整体而言，实验具有一定规模，但缺乏统计可靠性和组件级消融，因此充分性有限。

### 6. 论文的主要结论与发现
- ToolGraph 将加权平均奖励从 0.304 提升至 0.338（+11.2%），TG+DPO 进一步升至 0.355（+16.8% 相对基线）。
- 提升集中在航空（0.600→0.667→0.713）和零售（0.219→0.289→0.319）领域；电信变化不大（0.465-0.480），银行仍很低（~0.06-0.07）。
- 错误分析显示：ToolGraph 显著减少了 MAX_STEPS 和低奖励错误。电信的主要瓶颈是步骤预算耗尽（约一半轨迹在动作执行前结束），而非动作准确性（一旦达到动作阶段，准确率可达 95%+）。
- DPO 训练分析：**所选奖励的正向性**（chosen reward positivity）是最有用的检查点选择信号，优于准确率或边际；负的所选奖励会导致灾难性遗忘。
- 提示对齐的重要性：v1 因训练/部署提示不匹配导致奖励下降，v2 通过保持一致改善。
- 银行领域对 9B 模型仍然具有挑战性，即使前沿模型也仅约 25% Pass 1。

### 7. 优点
- **方法创新**：ToolGraph 结合模式拓扑和来自成功轨迹的经验权重，提供结构化推理指导；DPO 在相同上下文中训练，避免提示不匹配。
- **数据高效**：仅利用智能体自身的 532 条成功轨迹构造 ToolGraph 权重和 161 个偏好对，无需外部专家数据。
- **诊断发现**：揭示电信瓶颈并非动作错误而是步骤预算耗尽，以及所选奖励正向性比常见指标更可靠，为 DPO 训练提供实用指导。
- **系统实现**：完整闭环（执行层、控制层、仪表层），可复现。

### 8. 不足与局限
- **泛化性有限**：所有轨迹经验和评估来自同一 tau2-bench 任务集，未测试到未见任务或领域。
- **组件消融缺失**：ToolGraph 的多个机制（edge weights、pre-act gate、loop detection 等）被联合评估，无法确定各自独立贡献。
- **手动设计**：阶段分类、关键规则、评分参数为手动指定而非自动学习。
- **统计可靠性不足**：仅单次基线运行，没有多次运行报告误差或标准差；DPO 配置搜索非全因子。
- **设备资源细节缺失**：未明确 GPU 型号、数量、训练时间，可复现性和资源需求评估受限。
- **模型单一**：仅使用 Qwen 3.5 9B，未验证其他规模或架构。
- **银行领域表现极低**：方法在此领域几乎无效，需要更大模型或更专门的设计。

（完）
