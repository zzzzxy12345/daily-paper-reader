Title: A Reactive Redistribution Mechanism for STL Tasks in Multi-Agent Systems Under Time-Varying Communication

URL Source: https://arxiv.org/pdf/2606.13479v1

Published Time: Fri, 12 Jun 2026 01:08:42 GMT

Number of Pages: 9

Markdown Content:
# A Reactive Redistribution Mechanism for STL Tasks in Multi-Agent Systems Under Time-Varying Communication 

Gregorio Marchesini, Bjarne Jan Jesse Moro, Siyuan Liu, Lars Lindemann and Dimos V. Dimarogonas 

Abstract — We present a communication-aware task decom-position framework for multi-agent systems with collaborative relative configuration objectives specified in Signal Temporal Logic (STL), allowing for dynamic task reallocation under time-varying communication networks. Building on our prior work, the framework supports the direct use of existing feedback controllers for reactive task satisfaction. We address two key challenges: disjunctive STL specifications and time-varying communication networks. Disjunctive specifications are handled through a graph transition system that captures the alternative task sequences induced by logical OR operators. To address time-varying connectivity, we introduce a redistribution mecha-nism that transfers tasks from disconnected agents to connected ones as the network evolves while preserving decentralized execution. Simulations and experiments on a swarm of Crazyflie drones demonstrate scalability in the number of agents, com-munication connectivity, and specification complexity. 

I. I NTRODUCTION 

A broad class of multi-agent system (MAS) applica-tions requires enforcing prescribed configurations (forma-tions) among agents, such as drone light shows [1] or search and rescue missions [2]. Classical MAS control has been extensively studied under static specifications; however, emerging applications increasingly require the satisfaction of time-varying collaborative objectives involving explicit temporal constraints. Signal Temporal Logic (STL) [3] has become a standard formalism for specifying such spatio-temporal behaviors, as it allows for both formal verification and controller synthesis within a unified framework. Exist-ing methods for enforcing STL specifications in MAS can be broadly categorized into open-loop and feedback-based approaches. Open-loop approaches translate STL specifica-tions into mixed-integer linear constraints, yielding trajectory synthesis problems that can be solved via mixed-integer programming [4], [5]. While these methods are sound and complete, their computational complexity scales poorly with the number of agents and specifications, generally restrict-ing their applicability to small-scale systems. In contrast, feedback-based approaches address scalability limitations by embedding STL constraints into differentiable, time-varying inequality constraints, commonly via Control Barrier                         

> This work was supported in part by the Horizon Europe EIC project SymAware (101070802), ERC LEAFHOUND Project, Swedish Research Council (VR), and the Knut and Alice Wallenberg (KAW) Foundation. G. Marchesini, B. J. J. Moro, and D. V. Dimarogonas are with the Division of Decision and Control Systems, KTH Royal Institute of Technology, Stockholm, Sweden. E-mail:

{gremar,bjjmoro,dimos }@kth.se . S. Liu is with the Department of Electrical Engineering, Eindhoven University of Technology, the Netherlands. E-mail: s.liu5@tue.nl . L. Lindemann is with the Automatic Control Laboratory, ETH Z¨ urich, Z¨ urich, Switzerland. E-mail: 

> llindemann@ethz.ch .

Fig. 1: Crazyflie drones achieving a relative configuration task under time-varying communication. On the left, the edges of the communication graph are shown in red, while the required relative configurations are shown as cyan hyper-rectangles with yellow arrows representing their relative configuation. Full video available at https://youtu.be/qLjHKBQRmYw 

Functions [6] or Prescribed Performance Funnels [7]. This reformulation enables the synthesis of feedback controllers either analytically or by solving convex quadratic programs. Compared to open-loop approaches, these methods improve scalability with the number of agents, exhibit inherent robust-ness due to their reactive nature, and handle a rich fragment of STL specifications, at the expense of formal completeness, i.e., they may fail to find a solution even when one exists. A fundamental yet often implicit assumption in feedback-based multi-agent STL control is the compatibility of task dependencies with the underlying communication topology. Specifically, when a specification couples multiple agents, those agents must be able to exchange state information to enforce the associated constraints. In practical settings, however, communication is typically limited by distance-based constraints, and therefore, task-coupled agents may not be directly connected. This mismatch renders certain specifi-cations unrealizable under purely local feedback laws and has been typically addressed by two main approaches: distributed state estimation [8] and task decomposition [9]. Among these methods, task decomposition reformulates tasks defined over non-communicating agents into a set of specifications over subsets of agents connected in the communication graph that imply the original task. In this work, we propose a task decomposition frame-work, building upon our results in [9], to satisfy spatio-temporal relative configuration constraints, expressed as STL formulae, under time-varying communication graphs. Our contributions compared to [9] are as follows: first, we con-sider task decomposition over disjunctive type specifications, thereby enlarging the class of specifications that can be addressed. Second, we consider dynamic communication graphs among the agents, which naturally arise in real-world 

> arXiv:2606.13479v1 [eess.SY] 11 Jun 2026

practical scenarios. Third, we provide a scalability analysis with respect to the number of agents, the complexity of the STL task, and the connectivity of the communication graph. Finally, simulation studies are complemented by experimen-tal validation on a swarm of Crazyflie drones (see Fig. 1) 

Organization: Sec. II introduces the required preliminaries on STL for the specification of collaborative relative spa-tiotemporal tasks. Sec. III presents the problem formulation. Secs. IV and V develop the graph transition system and task decomposition as the main ingredients for satisfying relative STL tasks under time-varying range-limited communication. The overall integrated algorithmic framework is presented in Sec. VI, while experimental validation and conclusions are provided in Sec. VII and VIII, respectively. 

Notation: Real and non-negative real numbers are in-dicated as R and R≥0, respectively. Column vectors are indicated in bold, and x[k] is the k-th entry of x. The matrix Id n ∈ Rn×n is the identity matrix of size n. For x, y ∈ Rn,the Hadamard product is x⊙y = [ x[1] ·y[1] , . . . x[n]·y[n]] T .For a vector x, ∥x∥ is the standard 2-norm. The notation |C| 

denotes the cardinality of the set C, while A ⊕ B = {a + b |∀a ∈ A , b ∈ B} is the Minkowski sum of A ⊆ Rn and 

B ⊆ Rn. The set 2A is the power set of A, i.e., the set of all subsets of A. For vectors ai ∈ Rni with index i in the set K, the vector a = [ ai]i∈K ∈ RP  

> i∈K ni

is the stacked column vector of all vectors ai.II. P RELIMINARIES 

Consider a set of agents with indices in V = {1, . . . N },where each agent follows a nonlinear input-affine dynamics 

˙xi(t) = fi(xi(t)) + gi(xi(t)) ui(t), (1) such that xi ∈ Rnx and ui ∈ U ⊂ Rnu are the state and control input of agent i, respectively. We consider U ⊂ Rnu

to be compact, while fi : Rnx → Rnx and gi : Rnx → Rnu

are locally Lipschitz continuous in their arguments. Given the system’s state x = [ xi]i∈V ∈ RN n x , and assuming each agent is subject to a measurable input signal ui : [ t0, t 1] →

U, starting from the initial state x0, we denote by x(t) = 

qsol (t; x0, {ui}i∈V ), ∀t ∈ [t0, t 1] the unique maximal state solution 1 to (1), where x(t0) = x0.We define the relative state between agents i, j ∈ V as 

eij := xj − xi, eij ∈ Rnx . Moreover, pi = Sxi is the position of agent i, where S is an appropriate selection matrix 2, such that pij := pj − pi = Seij is the relative position of i and j. A static undirected graph G(V, E) ∈ Γ

is a tuple of the agents’ indices V (vertices of the graph) and a set of edges E ⊆ V × V , such that (i, j ) ∈ E ⇔ (j, i ) ∈ E .The set Γ is the set of undirected graphs on V. The set of 

neighbours of agent i is defined as Ni := {j ∈ V | (i, j ) ∈E} . When E is clear or irrelevant, we adopt the shorthand notation G := G(V, E), and we write (i, j ) ∈ G for (i, j ) ∈ E .                       

> 1Without loss of generality we assume in this work that all solutions to (1) under a measurable and bounded input signal ui: [ t0, t 1]→Uare defined over the whole interval [t0, t 1].
> 2For given input size nand output size m, a selection matrix S∈{0,1}n×msatisfies Pnj=1 S[i, j ] = 1 for all i, such that in y=Sx,the vector yrepresents a selection of the elements of x.

A directed path πij between agents i and j in G is defined as a sequence of L non-repeated indices in V such that πij := k1 k2 k3 . . . k L. Namely, letting πij [k] ∈ V 

indicate the k-th element of πij we have πij [1] = i,

πij [L] = j, (πij [k], πij [k + 1]) ∈ E . The function ϵ : πij 7 →{(πij [k], πij [k + 1]) }L−1 

> k=1

associates to each path πij the set of consecutive edges along the path. Similarly, a cycle πω

is defined as a sequence of non-repeated indices, except the initial and final nodes, i.e., πω [1] = πω [L].Beyond static graphs, we define a dynamic graph as a function G : R≥0 → Γ, t 7 → G(V, E(t)) , where E :

R≥0 → 2V×V is a function of time. In particular, we define the dynamic communication graph Gc(V, Ec(t)) with time-varying edges Ec(t) = {(i, j ) ∈ V × V | ∥ pij (t)∥ ≤ r} and neighbouring set Ni,c (t) for each i ∈ V .

A. Relative Configurations and Signal Temporal Logic 

For a desired relative vector cij ∈ Rnx and a given 

size vector αij ∈ Rnx

> ≥0

, we defined a relative configuration 

Hij (ηij ) ⊂ Rnx between i and j, with ηij = [ cTij , αTij ]T ∈

Rnx × Rnx

> ≥0

, as a shifted hyper-rectangle: 

Hij (ηij ) := {eij | | eij [k] − cij [k]| ≤ αij [k],

∀k = 1 , . . . n x} = {eij | H(eij − cij ) − M αij ≤ 0} (2) where H = [ Id nx , −Id nx ]T , M = [ Id nx , Id nx ]T . Notably, hyper-rectangles are closed under Minkowski summation, i.e., the Minkowski sums of hyper-rectangles are themselves hyper-rectangles. In particular, given a graph G and a path of agents πij in G, where each pair (r, s ) ∈ ϵ(πij ) is subject to ers ∈ H rs (ηrs ), then it holds [9, Prop. 3] 

M 

> (r,s )∈ϵ(πij )

Hrs (ηrs ) = Hij 

 X 

> (r,s )∈ϵ(πij )

ηrs 

 . (3) For a given relative configuration Hij (ηij ) we define a 

boolean predicate as 

μij = μij (eij ) := 

⊤ eij ∈ H ij (ηij ),

⊥ otherwise (4) and, for a subset E ⊆ V × V of agents’ pairs, with stacked relative states eE = [ ers ](r,s )∈E , we define θ(eE ) = V 

> (r,s )∈E

μrs , such that θ = ⊤ ⇔ ers ∈ H rs (ηrs ), ∀(r, s ) ∈E. In other words, θ encodes a Boolean satisfaction (True or False) of a set of relative configurations of the agents’ pairs in E. By this formalism, we can represent time-constrained relative configurations among agents using STL formulas as: 

φ := GI θ | FI θ, ψ := φ | ψ1 ∧ ψ2 | ψ1 ∨ ψ2, (5) where the operators GI and FI represent the always and 

eventually temporal operators over the time interval I ⊂

R≥0, while the operators ∧ and ∨ represent the logical conjunction and disjunction, respectively. We will denote by 

Ψ the set of formulas in the fragment (5). For notational convenience, we hereafter denote by ψ∧ formulas that only 

contain conjunctions as ψ∧ := φ1 ∧ φ2 ∧ . . . = V 

> k

φk,and we use the notation μij ∈ ψ to denote that a predicate 

μij is contained in the nested formula ψ. For a signal x : R≥0 → RN n x describing the state evolution of the multi-agent system under the dynamics in (1), the following semantic rules specify when each formula is satisfied 

x |= FI θ ⇔ ∃ τ ∈ I = [ a, b ] : θ(eE (τ )) = ⊤;

x |= GI θ ⇔ ∀ τ ∈ I = [ a, b ] : θ(eE (τ )) = ⊤;

x |= ⃝kφk ⇔ ⃝ k x |= φk, ⃝ ∈ {∨ , ∧} ,

(6) where x |= □ denotes that the state signal x : R≥0 → RN n x

satisfies a formula □. While the intervals I can potentially take any value, we make the following assumption: 

Assumption 1. Let I = {Ipi ⊂ R≥0}pnp 

> p1=1

, with np ≥ 1,be an ordered collection of disjoint intervals Ipi = [ api , b pi ]

such that pi < p j ⇒ bi < a j . Each task φ, as defined in (5) ,is of the form φ = TIpi θ, where T ∈ { G, F } and Ipi ∈ I .

In practice, Assumption 1 implies that a formula ψ consists of conjunctions/disjunctions of subformulas φ, sequentially ordered over the intervals in I. While this may not al-ways hold, it reflects standard engineering practice where a high-level planner schedules tasks over I. For a general task ψ, we define the task horizon and task activation as 

thr (ψ) = max pi bpi and tac (ψ) = min pi api , such that, by the semantics in (6), values of x(tac (ψ) − τ ) and x(thr (ψ) + τ ),for any τ ≥ 0, do not affect the satisfaction of ψ.

B. Task Graphs 

To coherently capture both task and communication cou-pling among agents, we represent a task ψ as a task graph ,which will enable direct comparison with Gc(t) and reveal structural mismatches that may hinder the satisfaction of ψ

via feedback, as analyzed shortly in Sec. III. Specifically, let the graph mapping G : Ψ → Γ, defined as 

G(ψ) = G(V, Eψ ); Eψ = {(i, j ) ∈ V × V | ∃ μij ∈ ψ}. (7) In other words, G(ψ) associates a task ψ with a correspond-ing static graph G(V, Eψ ), a task graph , that defines the task couplings among agents. Practically, two agents are coupled in G(ψ) if there exists a relative configuration predicate μij 

defined over their relative state eij in ψ. We denote by E(ψ)

the set of edges of the task graph G(ψ), and by Ni(ψ) the set of neighbors of agent i in G(ψ). For a given agent i, we define the state xNi(ψ∧) := [ xj ]j∈Ni(ψ∧), representing the stacked states of the neighbors of i in the task graph G(ψ∧).Furthermore, for presentation, we introduce the nota-tional equivalences: x |= G(ψ) ≡ x |= ψ; x |=

G(ψ1)G(ψ2) . . . ≡ x |= ψ1 ∧ ψ2 ∧ . . . , such that satisfying a sequence of task graphs G(ψ1)G(ψ2) . . . corresponds to satisfy their conjunction. III. P ROBLEM FORMULATION 

Recent works [10] studied decentralized feedback laws to satisfy conjunctions of tasks ψ∧ with the general form 

ui⟨τ, G(ψ∧)⟩ = κi(xNi(ψ∧)(t), t ), ∀t ∈ [τ, t hr (ψ∧)] (8) where κi : Rnx·| Ni(ψ∧)| × R≥0 → U is a function that maps a time t ∈ [τ, t hr (ψ∧)] and the state of the neighboring agents xNi(ψ∧) in the task graph G(ψ∧) to an input ui

for agent i (we show shortly how we adopt (8) to satisfy tasks ψ with disjunctions). The notation ui⟨τ, G(ψ∧)⟩ is used to denote that the feedback law in (8) is parametrized 

in both the task graph G(ψ∧) and the time τ ∈ [0 , t ac (ψ∧)) 

at which the feedback law starts to be applied to the system to satisfy ψ∧. In practice, ui⟨τ, G(ψ∧)⟩ : [ τ, t hr (ψ∧)] →

U is designed such that, starting from an initial state 

x(τ ), the state signal x : [ τ, t hr (ψ∧)] → RN n x , x(t) = 

qsol (t; x(τ ), {ui⟨τ, G(ψ∧)⟩} i∈V ), satisfies the task ψ∧, i.e., 

x |= ψ∧. Nevertheless, a key requirement to implement 

ui⟨τ, G(ψ∧)⟩ is that agent i must access xNi(ψ∧)(t), ∀t ∈

[τ, t hr (ψ∧)] , which demands Ni(ψ∧) ⊆ Ni,c (t), ∀t ∈

[τ, t hr (ψ∧)] , ∀i ∈ V , i.e., every neighbor in the task graph must be a neighbor in the communication graph until the time horizon thr (ψ∧). By communication consistency we formalize two situations for which this can not be guaranteed. 

Definition 1. For some t ≥ 0 and communication graph 

Gc(t), a predicate μij with relative configuration Hij (ηij )

is communication inconsistent if either of the conditions 

(i, j ) /∈ G c(t) (9) Hij (ηij ) ∩ C ij = ∅ (10) 

hold, with Cij = {eij ∈ Rnx | gc(eij ) = ∥pij ∥ − r ≤

0}, where r > 0 is the communication radius. A task 

ψ∧ (task graph G(ψ∧)) is communication inconsistent if it contains any such predicate (i.e., μij ∈ ψ∧); otherwise, it is communication consistent. 

Informally, by Def. 1, a task ψ∧ is inconsistent at time t

if: 1) the agents involved in ψ∧ are not in communication, as per (9) (indeed, (9) implies Ni(ψ∧)̸ ⊆ N i,c (t) by definition of the neighbouring set), or 2) it contains a predicate μij 

whose relative configuation Hij (ηij ) is inherently infeasible with the maximum communication distance r. Indeed, the condition Hij (ηij ) ∩ C ij = ∅ implies that if μij = ⊤,i.e, eij ∈ H ij (ηij ), then necessarily ∥pij ∥ > r . Thus (10) implies that (9) must hold at some time τ > t , as μij must take value ⊤ at some future time τ to satisfy ψ∧.Although communication consistency is time-dependent, we assume that if a task ψ∧ is consistent at some time t ≤

tac (ψ∧), then there exists a controller that both satisfies the task and preserves consistency for all τ ∈ [t, t hr (ψ∧)] .

Assumption 2. Consider a task ψ∧, a time τ < t ac (ψ∧), and an initial state x(τ ). Let ψ∧ be communication consistent with Gc(τ ). Then there exists a control law ui⟨τ, G(ψ∧)⟩ :[τ, t hr (ψ∧)] → U as per (8) with corresponding so-lution x(t) = qsol (t; x(τ ), {ui⟨τ, G(ψ∧)⟩} i∈V ), ∀t ∈

[τ, t hr (ψ∧)] , such that G(ψ∧) ⊆ G c(t) for all t ∈ [τ, t hr (ψ∧)] ,and x |= ψ∧.

Assmp. 2 states that if G(ψ∧) is communication consis-tent with Gc(τ ), then a controller exists that preserves the communication edges E(ψ∧) ⊆ E c(t) for all interval times 

t ∈ [τ, t hr (ψ∧)] , i.e., until the task is satisfied. Controllers with this property for the input affine dynamics in (1) are available in prior work [6], [10]–[12], assuming G(ψ∧)

is communication consistent. Indeed, we highlight that: 1) communication maintenance could itself be represented as a relative configuration of the form (4), which can then be included within the STL task ψ∧, 2) according to our defini-tion, communication consistency of a configuration Hij (νij )

requires compatibility with the communication radius r as per (10) in Def. 1, such that the communication and relative configuration requirements within G(ψ∧) are compatible .Motivated by this analysis, consider a MAS subject to a global STL task ψglob as per (5) and connected through the dynamic communication graph Gc(t). We satisfy ψglob under the communication constraints of the distributed law (8) via a two-step task decomposition framework. First (Sec. IV), we encode ψglob as a graph transition system (GTS), i.e., a transition system whose states are task graphs G(ψ∧pi ),where ψ∧pi is a conjunction of tasks over a common interval 

Ipi ∈ I (Assmp. 1). Satisfaction of the global task is then shown to be equivalent to satisfying a sequence of task graphs x |= G(ψ∧p1 ), . . . , G(ψ∧pnp ) along the GTS. In principle, this can be achieved by sequential application of controllers of the form (8). However, individual graphs 

G(ψ∧pi ) may violate communication consistency. Hence, in Sec. V, we decompose each G(ψ∧pi ) reactively into a communication consistent task graph G( ˜ψ∧pi ) (Def. 1) while preserving correctness ( x |= G( ˜ψ∧pi ) ⇒ x |= G(ψ∧pi )), thus enabling the satisfaction of ψglob via feedback. IV. G RAPH TRANSITION SYSTEM 

In this section, we show how disjunctions in a global task ψglob lead to alternative sequences of the task graphs that can satisfy ψglob . Namely, using the logical distributive/associative rules 3 for the ∧/∨ operators, we can rewrite ψglob in Disjunctive Normal Form (DNF) as ψglob = WKk=1 ψ∧ 

> k

for some K ≥ 1, where the tasks 

ψ∧ 

> k

:= φ1,k ∧ φ2,k ∧ . . . only contain conjunctions of tasks 

φ. Since each task φ is defined over an interval Ipi ∈ I 

(cf. Assmp. 1), we split each ψ∧ 

> k

in periods as ψ∧ 

> k

=(φp1 

> 1,k

∧ φp1 

> 2,k

. . . )

| {z } 

> ψ∧p1
> k:=

∧ (φp2 

> 1,k

∧ φp2 

> 2,k

. . . )

| {z } 

> ψ∧p2
> k:=

∧ . . . (φpnp  

> 1,k

∧ φpnp  

> 2,k

. . . )

| {z } 

> ψ∧pnp k:=

where each task ψ∧pi 

> k

, ∀pi = p1, . . . p np is a conjunction of tasks φpi = TIpi θ, T ∈ { G, F }. Thus, we write ψglob as 

ψglob = _Kk=1 



ψ∧p1 

> k

∧ ψ∧p2 

> k

∧ . . . ∧ ψ∧pnp 

> k



. (11) The form (11) clarifies that satisfaction of ψ∧ 

> k

corresponds to the satisfaction of a sequence of tasks ψ∧pi 

> k

. Namely, recalling the semantics in (6), if an index k exists such that x |= G(ψ∧p1 

> k

)G(ψ∧p2 

> k

) . . . G(ψ∧pnp  

> k

) then x |= ψglob .Henceforth, a first approach to satisfy ψglob is to directly choose one of the K sequences of tasks and try to satisfy it using a feedback controller of the form (8). However, such an approach would not be reactive to unexpected contingencies in the satisfaction of ψglob , as shown in the next example: 

Example 1. Let a global task ψglob in DNF ψglob = ( ψ∧p1 

> 1

∧

ψ∧p2 

> 1

) ∨ (ψ∧p1 

> 2

∧ ψ∧p2 

> 2

) and assume ¯ψ∧p1 = ψ∧p1 

> 1

= ψ∧p1

> 2

for some common task ¯ψ∧p1 . Equivalently we write ψglob as 

ψglob = ¯ψ∧p1 ∧ (ψ∧p2 

> 1

∨ ψ∧p2 

> 2

). Looking at ψglob using our graph intuition, then satisfaction of ψglob is equivalent to first satisfying the graph G( ¯ψ∧p1 ) and then either G(ψ∧p2 

> 1

)                       

> 3(a∧b)∨c= ( a∨c)∧(b∨c),(a∨b)∧c= ( a∧c)∨(b∧c)

or G(ψ∧p2 

> 2

). This shows a subtle, but important difference: before factoring ¯ψ∧p1 , we had to decide a priori which of the two sequences ( ¯ψ∧p1 ∧ψ∧p2 

> 1

) or ( ¯ψ∧p1 ∧ψ∧p2 

> 2

) would be satisfied. After factorization, however, the decision between 

ψ∧p2 

> 1

and ψ∧p2 

> 2

can be postponed until after the satisfaction of the common task graph G( ¯ψ∧), enabling reactive selection if either ψ∧p2 

> 1

or ψ∧p2 

> 2

becomes infeasible (e.g., due to obstacles or agent failure). 

Following these arguments, we introduce the notion of a graph transition system (GTS). This abstraction will enable representing sequences of task graphs satisfying ψglob as runs 

of a suitably defined transition system, which we compute 

online rather than fix a priori (Sec. VI). 

Definition 2. A graph transition system is a tuple 

Ω( ˆΓF , {ˆΓpi }pnp  

> pi=p1

, A) where ˆΓpi ⊂ Γ, ∀pi = p1, . . . p np ,

ˆΓF ⊂ Γ is the set of terminal graphs, and A : ˆΓpi → 2ˆΓpi+1 

is a transition relation. A run σ of Ω is a finite sequence of graphs σ = G1G2 . . . GL, with σ[k] denoting the k-th graph. A run σ belongs to the language LΩ, σ ∈ L Ω, if σ[1] ∈ ˆΓp1 ,

σ[L] ∈ ˆΓF , and σ[k + 1] ∈ A (σ[k]) for all k = 1 , . . . , L − 1.

The GTS associated with a task ψglob is constructed recursively (Alg. 1) by exploiting the distributivity of 

∨/∧ to group tasks defined over common periods (cf. Example 1). Namely, we start with a task ψglob =

W

> k∈K



ψ∧p1 

> k

∧ ψ∧(pi>p 1)

> k



, K = {1, . . . , K }, where for brevity ψ∧(pi>p 1) 

> k

:= ψ∧p2 

> k

∧ · · · ∧ ψ∧pnp  

> k

, and we initialize an empty automaton Ω. The index set K is partitioned into disjoint subsets {K l}Ml=1 (Line 4) such that ∪lKl = K.Each subset groups indices sharing the same first-period task ¯ψ∧p1 

> Kl

:= ψ∧p1 

> k

for all k ∈ Kl and, using dis-tributivity, we have W

> k∈K l

 ¯ψ∧p1 

> Kl

∧ ψ∧(pi>p 1)

> k



= ¯ψ∧p1 

> Kl

∧

W 

> k∈K l

ψ∧(pi>p 1) 

> k

. A graph G( ¯ψ∧p1 

> Kl

) is then added as a state in the GTS by adding it to ˆΓp1 (Line 6) and a transition is defined (Line 9) from G( ¯ψ∧p1 

> Kl

) toward the task graphs generated by the residual task ψnext = W 

> k∈K l

ψ∧(pi>p 1) 

> k

,

constructed recursively by the same procedure. The recursion proceeds until all periods are exhausted; nodes with empty transition sets are marked as terminal (Line 10). Thus, solely using the distributivity property, the following lemma holds. 

Lemma 1. Consider a task ψglob with GTS Ω as per Alg. 1. For each run σ ∈ L Ω it holds that x |= σ ⇒ x |= ψglob .

Algorithm 1 Construction of the GTS  

> 1:

set ψglob = W

> k∈K

(ψ∧p1 

> k

∧ . . . ψ ∧pnp  

> k

), K = {1, . . . K } 

> 2:

set {ˆΓpi }pnp  

> pi=p1

, ˆΓF , A ← ∅  

> 3:

Function MakeNodes( ψ = W

> k∈K

(ψ∧pi 

> k

∧ ψ∧(pj >p i) 

> k

)) 

> 4:

Partition K into groups {K l} with identical ψ∧pi 

> Kl
> 5:

for each group Kl do  

> 6:

ˆΓpi ← ˆΓpi ∪ { G(ψ∧pi 

> Kl

)} 

> 7:

if pi < p np then  

> 8:

ψnext = W 

> k∈K l

ψ∧(pj >p i) 

> k

, 

> 9:

A(G(ψ∧pi 

> Kl

) ← MakeNodes (ψnext ) 

> 10:

if A(G(ψ∧pi 

> Kl

)) = ∅ then ˆΓF ← ˆΓF ∪ { G(ψ∧pi 

> Kl

)} 

> 11:

return {G(ψ∧pi 

> Kl

)}lIn Section VI, we will use the transition relation A of the GTS Ω corresponding to a global task ψglob to satisfy the given task reactively (cf. Example 1). V. T ASK DECOMPOSITION OVER GTS By Lemma 1, we have related the satisfaction of ψglob 

to finding a run σ ∈ L Ω. As explained in greater detail in Sec. VI, we can do so by a sequential approach by starting from the satisfaction of a task graph G(ψ∧p1 ) ∈

ˆΓp1 , and then a task graph G(ψ∧p2 ) ∈ A (G(ψ∧p1 )) , and continue recursively to satisfy G(ψ∧pi ) ∈ A (G(ψ∧pi−1 )) 

until G(ψ∧pi ) ∈ ˆΓF . Although this procedure alone does not simplify feedback synthesis directly, it structures the problem as satisfying a sequence of task graphs. At the feedback level, we must ensure that task sequences are consistent with the time-varying communication graph Gc(t). To this end, each task ψ∧pi is decomposed into a communication consistent task ˜ψ∧pi such that 

x |= G( ˜ψ∧pi ) ⇒ x |= G(ψ∧pi ). At a high level, this is achieved by decomposing each inconsistent relative predicate μij into a sequence of relative predicates ˜μij rs 

over the edges (r, s ) ∈ ϵ(πij ) of a path πij connecting i

to j in Gc(t) (Section V-A). As we show next, we are able to compute such predicates ˜μij rs via convex optimization (Section V-B). Note that we already approached this problem in detail in [9], where a more detailed analysis of the problem is found. In the presentation, we denote the set of inconsistent edges at time t for ψ∧pi as I(t, ψ ∧pi ) := 

{(i, j ) | ∃ μij ∈ ψ∧pi such that (9) or (10) hold }, whereas consistent edges are given by E(ψ∧pi ) \ I(t, ψ ∧pi ).

A. Task rewriting via parametric predicates 

Lemma 2 and Corollary 1 clarify how our decomposition approach is defined, using paths of connected agents in Gc(t).

Lemma 2 ([9]) . For time t and a predicate μij with 

Hij (ηij ), let a directed path πij from i to j in Gc(t). Let the conjunction 

˜θij (eϵ(πij )) := ˜θij = ^  

> (r,s )∈ϵ(πij )

˜μij rs , (12) 

with, eϵ(πij ) = [eij ](i,j )∈ϵ(πij ), and ˜Hij rs ( ˜ ηij rs ) being the formation sets for each ˜μij rs as per (4) , such that 

M 

> (r,s )∈ϵ(πij )

˜Hij rs ( ˜ ηij rs ) ⊆ H ij (ηij ). (13) 

Then ˜θij = ⊤ ⇒ μij = ⊤.

Corollary 1. For time t and period Ipi , let ψ∧pi = ∧kφpi 

> k

,where φpi 

> k

= TIpi θk = TIpi

V 

> (i,j )∈E k

μij 



, with T ∈{G, F }, such that Ek ⊆ E(ψ∧pi ). Moreover, for all k, let 

˜φpi 

> k

:= TIpi ( ^  

> (i,j )∈E k\I(t,ψ ∧pi)

μij ∧ ^  

> (i,j )∈E k∩I(t,ψ ∧pi)

˜θij ), (14) 

where predicates μij , (i, j ) ∈ E k ∩I(t, ψ ∧pi ) are replaced by 

˜θij as per (12) . Then, x |= ˜ψ∧pi := V 

> k

˜φk ⇒ x |= ψ∧pi .

The results of Lemma 2 and Corollary 1 enable the construction of a task ˜ψ∧pi whose satisfaction implies that of ψ∧pi . Namely, for each inconsistent predicate μij with 

(i, j ) ∈ I(t, ψ ∧pi ), a decomposition path πij in Gc(t) is first identified. Such a path can be computed efficiently, for instance using a depth-first search with complexity O(V +

Ec(t)) [13, Sec. 29]. In general, multiple decomposition paths may exist for a given pair (i, j ); this selection is discussed in Remark 1. By the given paths selection, the task ˜ψ∧pi is obtained by replacing each μij with the conjunction ˜θij =V  

> (r,s )∈ϵ(πij )

˜μij rs , ϵ(πij ) ⊆ G c(t). The corresponding relative configuration sets ˜Hij rs ( ˜ ηij rs ) are then selected to satisfy the Minowsky inclusion (13) and the condition ˜Hij rs ( ˜ ηij rs ) ⊆ C rs ,where these constraints can be enforced by a set of convex constraints on the parameters ˜ηij rs (see Prop 1-2 in Appendix for details). By replacing all inconsistent predicates in such a manner, we obtain ˜ψpi as communication consistent (cf. Def. 1). While the proposed procedure resolves the communication consistency problem, we should additionally ensure that the resulting task ˜ψ∧pi is satisfiable (i.e., there exists a state tra-jectory x such that x |= ˜ψ∧pi ) or otherwise, no meaningful use of ˜ψ∧pi could be made to satisfy the communication inconsistent task ψ∧pi . This requires extra constraints on the selection of the relative configurations ˜Hij rs ( ˜ ηij rs ) as discussed in greater detail in [9, Sec. IV]. In particular, the relative predicates ˜Hij rs ( ˜ ηij rs ) should be chosen such that overloading 

of edges and circular dependancies are resolved: 

Overloading : When decomposing two predicates μij and 

μi′j′ , with (i, j ), (i′, j ′) ∈ I(t, ψ ∧pi ), it is possible that their decomposition paths πij , πi′j′ intersect, i.e., there exists 

(r, s ) ∈ ϵ(πij ) ∩ ϵ(πi′j′ ). Moreover, it is possible that a consistent predicate μrs , i.e., (r, s ) ∈ E(ψ∧pi ) \ I(t, ψ ∧pi ),is already present on (r, s ). In either case, we term the edge 

(r, s ) as overloaded . To resolve overloading, we first ensure that only a single parametric predicate ˜μij rs is defined for all paths πij such that (r, s ) ∈ ϵ(πij ). At the same time, if a consistent predicate μrs is already present on (r, s ),then we enforce the condition ˜Hij rs ( ˜ ηij rs ) ⊆ H rs (ηrs ) such that, by Def. 4, ˜μij rs = ⊤ ⇒ μrs = ⊤. In this way, the satisfaction of μrs is not compromised by the addition of ˜μij rs . Note that the inclusion ˜Hij rs ( ˜ ηij rs ) ⊆ H rs (ηrs ) is equivalent to a set of linear inequalities in the parameters 

˜ηij rs (see Appendix, Cor. 2). We denote the set of overloaded ACBD

Fig. 2: The four constraints required to achieve a task decompo-sition. (A) Minkowski inclusion constraint as per (13), (B) Task overloading when two tasks are decomposed along a common edge, (C) Communication Consistency constraint ˜Hij rs ( ˜ ηij rs ) ⊆ C ij , (D) Cycle closure constraint. edges as O(t, ψ ∧pi ) = {(r, s ) | (r, s ) ∈ ϵ(πij ) ∧ (r, s ) ∈

E(ψ∧pi ) \ I(t, ψ ∧pi ), (i, j ) ∈ I(t, ψ ∧pi )}.

Circular Dependencies : Decomposing ψ∧pi into ˜ψ∧pi

requires careful examination of circular task dependencies as specified in Lemma 3: 

Lemma 3. ( See [9, Fact 4-5]) Consider ψ∧pi = ∧kφpi 

> k

and let πω be a cycle in G(ψ∧pi ) of length L. If at least L − 1

predicates μrs , (r, s ) ∈ ϵ(πω ) are such that μrs ∈ φpi 

> k

=

GIpi θ for some k, then 0 /∈ L  

> (r,s )∈ϵ(πω)

Hrs (ηrs ) ⇒ x̸ |=

ψ∧pi , for any state trajectory x : R≥0 → RN n x .

Henceforth, satisfying the obtained task ˜ψ∧pi we require that for every cycle πω in G( ˜ψ∧pi ) containing at least L − 1

tasks with an always operator, the Minkowski sum of the relative predicates along πω must contain the origin (Fig. 2-D). To enforce this we introduce auxiliary variables ζaux  

> rs

∈

Rnx for each edge (r, s ) ∈ ϵ(πω ), subject to: 

X 

> (r,s )∈ϵ(πω)

ζaux  

> rs

= 0, ζaux  

> rs

∈ H rs (ηrs ), ∀ (r, s ) ∈ ϵ(πω ),

(15) where here Hrs (ηrs ) can be parametric (thus of the form 

˜Hij rs ( ˜ ηij rs )), or not. By definition of Minkowski sum, relation (15) certifies 0 ∈ L  

> (r,s )∈ϵ(πω)

Hrs (ηrs ), and reduce to linear inequalities in ζaux  

> rs

and ηrs . For presentation, we let 

C(t, ψ ∧pi ) be the set of cycles in G( ˜ψ∧pi ) obtained from the decomposition over the communication graph Gc(t).

B. Overall Optimization 

In summary, in the process of rewriting ψ∧pi into 

˜ψ∧pi at time t, assume that the maximum length of the paths πij used for the decomposition is L. It can be shown that, to achieve the task decomposition, we in-troduce a number of optimization variables Mvar propor-tional to Mvar = O(L (|C(t, ψ ∧pi )| nx + |I(t, ψ ∧pi )| 2nx)) 

(corresponding to the variables ˜ηij rs and ζaux  

> rs

), and a number of convex constraints Mcon proportional to 

Mcon = O((2 nx 2nx)|I(t, ψ ∧pi )| + 2 nx L|I(t, ψ ∧pi )| +(2 nx 2nx )|O(t, ψ ∧pi )| + ( nx + 2 nx)|C(t, ψ ∧pi )|). Consider now the vector ξ ∈ RMvar stacking all the decomposition variables and let g⟨t, ψ ∧pi ⟩ : RMvar → RMcon be the function stacking all the convex inequality constraints required to decompose ψ∧pi into ˜ψ∧pi (where the notation ⟨t, ψ ∧pi ⟩

applies to denote parametric dependence on t and ψ∧pi ). Then each vector ξ such that g⟨t, ψ ∧pi ⟩(ξ) ≤ 0 corresponds to a valid decomposition of ψ∧pi into ˜ψ∧pi at time t, for which x |= ˜ψ∧pi ⇒ x |= ψ∧pi , and ˜ψ∧pi is communication consistent. In particular, given the system state x(t) at time t, we look for solutions ξ∗ minimizing the convex cost f⟨t, ψ ∧pi ⟩(ξ) = P 

> (i,j )∈I(t,ψ ∧pi)

P  

> (r,s )∈ϵ(πij )

∥ers (t)−

˜cij rs ∥2 which represents the sum of the squared distances of the relative vectors of each new relative configuration 

˜Hrs ( ˜ ηij rs ), ˜ηij rs = [˜ cij rs , ˜αij rs ], ∀(r, s ) ∈ ϵ(πij ), ∀(i, j ) ∈

I(t, ψ ∧pi ) from the current relative positions ers (t). Infor-mally, f⟨t, ψ ∧pi ⟩(ξ) incentivizes the desired relative vectors of the relative configurations ˜Hij rs ( ˜ ηij rs ) in ˜ψ∧pi , (introduced by the decomposition) to be as close as possible to the actual relative state eij (t) of the agents at time t. To conclude, decomposing ψ∧pi into ˜ψ∧pi is achieved by solving the convex program : 

D⟨ t, ψ ∧pi ⟩ : min  

> ξ

f⟨t, ψ ∧pi ⟩(ξ) s.t. g⟨t, ψ ∧pi ⟩(ξ) ≤ 0, (16) and we say that ψ∧pi is decomposable if D⟨ t, ψ ∧pi ⟩ has a solution, and, with an abuse of notation, we write ˜ψ∧pi =

D⟨ t, ψ ∧pi ⟩ when ˜ψ∧pi is obtained by decomposing ψ∧pi at time t over the communication graph Gc(t) by solving (16). 

Remark 1. Feasibility of D⟨ t, ψ ∧pi ⟩ depends on the choice of decomposition paths πij . Longer paths introduce more constraints (e.g., overloading and circular dependencies), but must be sufficiently long to enable decomposition. In partic-ular, for a relative configuration Hij (ηij ), ηij = [ cij , αij ],decomposed over the relative configurations ˜Hij rs ( ˜ ηij rs ), the path length L should satisfy L ≥

l ∥cij ∥

> r

m

to ensure feasibility of the Minkowski constraint (13) , jointly with with the com-munication constraint ˜Hij rs ( ˜ ηij rs ) ⊆ C rs , ∀(r, s ) ∈ ϵ(πij ).

VI. O VERALL ALGORITHMIC IMPLEMENTATION 

Based on the GTS construction and the task decomposition procedure, the algorithm used to satisfy the global task ψglob 

is presented in Alg. 2. At a high level, the proposed method alternates between task decomposition and continuous-time execution of a feedback controller associated with a se-lected task graph. Specifically, the algorithm is initialized at t = 0 with the global task ψglob , its corresponding GTS Ω( ˆΓF , {ˆΓpi }pnp  

> pi=p1

, A), the initial state of the system 

x0, and the communication graph available at the initial time Gc(t) (Lines 1-2). By the definition of the GTS Ω, a set of candidate task graphs that may be executed at each period is denoted by ˆΓon and initialized as ˆΓon = ˆΓp1 .We use the set ˆΓon to find a σ ∈ L Ω in the GTS online 

by following the transition relation A of Ω starting by 

ˆΓon = ˆΓp1 . Namely, at the first iteration, the algorithm searches for a task ψ∧p1 whose corresponding task graph 

G(ψ∧p1 ) belongs to the admissible candidate set ˆΓon and is decomposable with respect to the current communication graph Gc(t) (Line 4). When such a task graph is found, the associated decomposed task graph G( ˜ψ∧p1 ) = D⟨ t, ψ ∧p1 ⟩ is selected for execution. Recalling that for any task ψ∧pi with interval Ipi = [ api , b pi ], the activation time and horizon are given by tac (ψ∧pi ) = api , t end (ψ∧pi ) = bpi , then a feedback controller ui⟨t, G( ˜ψ∧p1 )⟩ : [ t, b p1 ] → U is implemented on the system, satisfying Assmp. 2 (Line 7). Under this con-troller, the resulting state trajectory xp1 : [ t, b p1 ] → RN n x

with xp1 (τ ) = qsol 



τ ; x0, {ui⟨τ, G( ˜ψ∧p1 )⟩} i∈V 



∀τ ∈

[t, b p1 ] satisfies ˜ψ∧p1 , and thus, by Corollary 1, also satisfies 

ψ∧p1 . Moreover, by Assmp. 2, the graph G( ˜ψ∧p1 ) remains communication consistent with Gc(τ ) over the entire interval 

τ ∈ [t, b p1 ]. Once the execution interval ends at time τ =

bp1 , the algorithm updates the set of candidate task graphs that can be executed next as ˆΓon = A(G(ψ∧p1 )) ⊆ ˆΓp2 .Using this updated set, the algorithm repeats the same operations accomplished for the first period. Namely, atask graph G(ψ∧p2 ) decomposable over the current com-munication graph Gc(bp1 ) is searched for and a trajectory xp2 : [ bp1 , b p2 ] → RN n x is computed, under the feedback law ui⟨bp1 , G( ˜ψ∧p2 )⟩ : [ bp1 , b p2 ] → U, with G( ˜ψ∧p2 ) = 

D⟨ bp1 , G( ˜ψ∧p2 )⟩, such that xp2 |= ψ∧p2 and we update 

ˆΓon = A(G(ψ∧p2 )) ⊆ ˆΓp3 . This procedure is repeated sequentially for each period. Upon reaching a terminal graph in the GTS, the algorithm terminates, yielding a satisfying trajectory formed by the concatenation of the trajectories generated over each execution interval. By construction, this trajectory satisfies the sequence of task graphs σ =

G(ψ∧p1 ) G(ψ∧p2 ) G(ψ∧p3 ) . . . in LΩ.

Theorem 1. Consider a global task ψglob , the initial time 

t0 = 0 and initial state x0. The closed-loop trajectory 

x : [ t0, b np ] → RN n x obtained by Alg. 2 through the con-catenation of closed-loop trajectories x(t) = xpi (t), ∀t ∈

[bpi−1 , b pi ], i = 1, . . . , n p, with bp0 := t0, is such that 

x |= ψglob .

Henceforth, Alg. 2 provides a solution for feedback-based satisfaction of ψglob under time-varying communication con-straints, as per our problem formulation. 

Remark 2. In Alg. 2, we assumed at least one decomposable task G(ψ∧pi ) exists in ˆΓon per period. Infeasibility occurs when disconnected graphs Gc(t) lack decomposition paths 

πij for inconsistent predicates, or when excessive circular dependencies in G( ˜ψ∧pi ) render (16) unsolvable. To mit-igate this, agents can gather toward a common state to improve Gc(t) connectivity. Alternatively, for strict real-time requirements, we relax (16) to g⟨t, ψ ∧pi ⟩(ξ) ≤ λ for some 

λ ∈ RMcon 

≥0 and compute a minimum-violating solution. 

Algorithm 2 

1: set finished ← False, t ← 0, x0 → x

2: set Ω ← GT S (ψglob ), ˆΓon ← ˆΓp1

3: while not finished do 

4: for G(ψ∧pi ) in ˆΓon do 

5: Attempt G( ˜ψ∧pi

k ) = D⟨ t, ˜ψ∧pi

k ⟩ using (16) 

6: if problem is feasible then 

7: xpi (t) = qsol (τ ; x0, {ui⟨bpi−1 , G( ˜ψ∧pi )⟩} i∈V )

8: x0 ← xpi (bpi )

9: break 

10: else 

11: continue 

12: ˆΓon ← A (G(ψ∧pi )) ,

13: if ˆΓon = ∅ then finished → T rue 

VII. N UMERICAL EXPERIMENTS 

We evaluate our framework in terms of the scalability of the decomposition approach and hardware implementation. 

Scalability : To evaluate scalability, we measure the com-putation time required to decompose a task graph G(ψ∧pi )

on a communication graph Gc(V, Ec) by solving (16), in-cluding the graph operations needed to compute the paths 

πij and cycles πω . The results are shown as heatmaps in Fig. 3. Each row corresponds to a fixed number of agents 

N ∈ { 10 , 30 , 50 }. For each configuration, we generate a random communication graph Gc(V, Ec) with connectivity 0.1 0.5 0.9 % task                            

> 0.9 0.5 0.1 % comm
> 1.44 ±0.13 8.06 ±0.39 15.62 ±0.36 1.44 ±0.07 8.24 ±0.59 9.88 ±0.52 1.40 ±0.06 2.32 ±0.20 3.10 ±0.46
> 0.1 0.5 0.9 0.9 0.5 0.1 % comm
> 0.44 ±0.02 2.50 ±0.08 4.67 ±0.11 0.44 ±0.02 2.47 ±0.04 2.94 ±0.15 0.43 ±0.02 0.75 ±0.06 0.93 ±0.05
> 0.1 0.5 0.9 0.9 0.5 0.1 % comm
> 0.04 ±0.01 0.23 ±0.01 0.42 ±0.02 0.04 ±0.01 0.22 ±0.04 0.26 ±0.03 0.04 ±0.02 0.12 ±0.03 0.13 ±0.02
> %inconsistency : 0.1
> 0.1 0.5 0.9 %task
> 0.9 0.5 0.1
> 1.43 ±0.06 8.14 ±0.27 15.42 ±0.65 1.55 ±0.07 7.89 ±0.24 13.30 ±0.62 1.54 ±0.17 3.83 ±0.18 6.08 ±0.40
> 0.1 0.5 0.9 0.9 0.5 0.1
> 0.46 ±0.02 2.52 ±0.09 4.66 ±0.12 0.48 ±0.03 2.45 ±0.05 3.67 ±0.15 0.48 ±0.02 1.21 ±0.04 1.87 ±0.05
> 0.1 0.5 0.9 0.9 0.5 0.1
> 0.04 ±0.01 0.23 ±0.01 0.42 ±0.02 0.04 ±0.01 0.22 ±0.02 0.33 ±0.03 0.04 ±0.01 0.15 ±0.02 0.21 ±0.02
> %inconsistency : 0.3
> 0.1 0.5 0.9 %task
> 0.9 0.5 0.1
> 1.47 ±0.13 8.23 ±0.35 15.06 ±0.23 1.59 ±0.08 8.00 ±0.36 14.83 ±0.69 1.73 ±0.18 5.39 ±0.17 9.04 ±0.33
> 10 1Mean Time (s) 0.1 0.5 0.9 0.9 0.5 0.1
> 0.47 ±0.02 2.56 ±0.06 4.82 ±0.26 0.50 ±0.05 2.46 ±0.06 4.35 ±0.13 0.50 ±0.03 1.74 ±0.04 2.80 ±0.09
> 10 0Mean Time (s)
> 0.1 0.5 0.9 0.9 0.5 0.1
> 0.04 ±0.01 0.22 ±0.01 0.42 ±0.02 0.04 ±0.01 0.23 ±0.04 0.38 ±0.02 0.05 ±0.01 0.20 ±0.02 0.30 ±0.02
> %inconsistency : 0.5
> 10 1
> Mean Time (s)

Fig. 3: Scalability test. Computational time is reported in seconds. Experiments were conducted on a system equipped with an Intel Core i7-1265U processor and 32 GB of RAM. 

parameter pcomm ∈ {0.1, 0.5, 0.9} (y-axis), selecting ran-domly |E c| = ⌈pcomm N (N −1) ⌉ edges. A task graph G(ψ∧pi )

is then generated with edges |E(ψ∧pi )| = ⌈ptask N (N − 1) ⌉

randomly selected for parameter ptask ∈ { 0.1, 0.5, 0.9} (x-axis). A fraction pinc ∈ {0.1, 0.3, 0.5} of these edges is enforced communication inconsistent (columns in Fig. 3), while the remaining edges are selected to be communication consistent up to the available communication edges Ec. For each pair (ptask , p comm ), we generate 20 random instances of the decomposition problem and report the average com-putation time. In all experiments, the time spent on graph operations, to find decomposition paths and cycles, domi-nates the time required to solve (16), which for all settings is below 0.5s. From Fig. 3, the computational time is mainly driven by task connectivity. When both communication and task connectivity are high, computation is dominated by the enumeration of cycles πω required to avoid infeasible tasks (Sec. V-A). The percentage of inconsistent tasks has a minor effect at high communication connectivity, but the impact is more pronounced at low communication connectivity, where longer decomposition paths are required to decompose inconsistent predicates in the task graph. 

Hardware experiments : We validated the framework pro-posed in Alg. 2 through laboratory experiments 4 using a swarm of seven Crazyflie drones integrated with ROS2 using the Crazyswarm library [14]. The experiments demonstrate the agents successfully achieving a global task ψglob as represented by the GTS in Fig. 4 and leveraging Alg. 2. In Fig. 4, each node signifies a possible task graph G(ψ∧pi ),where ψ∧pi is the conjunction of the individual tasks φ

4Video: https://youtu.be/qLjHKBQRmYw ; Code: https:// github.com/bjarnemoro/crazyflie_ros , https://github. com/gregoriomarchesini/faste_decomposition Ip1 = [25 , 30]                                                                                                                                                      

> φ=GI∥e1,11 −[2 ,0] ∥∞≤0.2
> φ=GI∥e1,3−[1 ,1.4] ∥∞≤0.2
> φ=GI∥e11 ,3−[−1,1.4] ∥∞≤0.1
> φ=GI∥x1−[0 ,0] ∥∞≤0.1
> Ip2= [75 ,80] φ=FI∥e1,6−[3 .4,0] ∥∞≤0.2
> φ=GI∥x1−[0 ,0] ∥∞≤0.2
> φ=FI∥e3,6−[3 .4,0] ∥∞≤0.2
> φ=GI∥x3−[0 ,0] ∥∞≤0.2
> Ip3= [140 ,160]
> φ=GI∥e6,4−[0 ,1] ∥∞≤0.2
> φ=GI∥e6,5−[0 ,−1] ∥∞≤0.2
> φ=GI∥e6,11 −[−1.7,0.4] ∥∞≤0.1
> φ=FI∥e6,10 −[−1.7,−0.4] ∥∞≤0.1
> φ=FI∥x6−[0 .5,0] ∥∞≤0.2
> φ=GI∥e4,6−[0 ,1] ∥∞≤0.2
> φ=GI∥e5,6−[0 ,−1] ∥∞≤0.2
> φ=GI∥e11 ,6−[−1.7,0.4] ∥∞≤0.1
> φ=FI∥e10 ,6−[−1.7,−0.4] ∥∞≤0.1
> φ=FI∥x4−[0 .5,0] ∥∞≤0.2

Fig. 4: Graph Transition System Ω for the experiments. 

within that node. The sequence σ selected to satisfy ψglob 

is highlighted in red. For brevity in Fig. 4 we wrote 

|eij − cij ∥∞ ≤ α to denote the relative configuration set 

Hij (ηij ) = {eij | H(eij − cij ) − M 1α} as per (2). VIII. C ONCLUSION 

We proposed a communication-aware STL decomposition framework for multi-agent formations. Using a graph transi-tion system and task redistribution, the approach maintains decentralized feedback-based reactivity under time-varying communication. Scalability across agent count and task com-plexity was verified in simulation and hardware experiments. Future work includes scaling experiments to larger swarms. APPENDIX 

In this section, we show that: 1) the inclusion Hij (η) ⊆Cij in a convex set Cij (e.g., the communication set in Def. 1) is equivalent to a set of convex constraints on 

η (Prop. 1-Cor. 2) 2) the Minkowski inclusion condition L

(r,s )∈ϵ(πij ) Hrs (ηrs ) ⊆ H ij (ηij ) (appearing in (13)) is equivalent to a set of linear inequalities in ηrs , ∀(r, s ) ∈

ϵ(πij ) (Prop. 2). 

Proposition 1. Let a relative configuration Hij (ηij ) and a set Cij := {eij ∈ Rnx | gk(eij ) ≤ 0, k = 1 , . . . n g },where ng ≥ 1 and each gk : Rnx → R is convex. Then 

Hij (ηij ) ⊆ C ij if an only if 

gk(vl(ηij )) ≤ 0, ∀l = 1 , . . . 2nx , ∀k = 1 , . . . n g . (17) 

where each vl : R2nx → Rnx , l = 1 , . . . 2nx takes the form 

vl(ηij ) = cij + αij 

2 ⊙ sl, sl ∈ {− 1, 1}nx . (18) 

Proof. (Sketch) By definition, the set Hij (ηij ) can be written as the convex hull [15, Sec. 2.2.4] of a set of 

nv = 2 nx vertices vl(ηij ) as Hij (ηij ) = {Pnv

l=1 λlvl(ηij ) |Pnv

l=1 λl = 1 , λ l ≥ 0}, where each vertex vl, l = 1 , . . . 2nx ,

is a linear function of ηij as per (18). By Jensen’s inequality, [15, Sec. 3.1.8], we can then conclude that Hij (ηij ) ⊆ C ij 

if and only if each vertex vl(ηij ) satisfies the inequality 

gk(vl(ηij )) ≤ 0 ∀k = 1 , . . . n g , which is exactly (17). △

Corollary 2. Given relative configurations Hij (ηij ) and 

H′

ij (η′

ij ), then Hij (ηij ) ⊆ H′

ij (η′

ij ) if an only if 

gk(vl(ηij )) := hTk (vl(ηij ) − c′

ij ) − mTk α′

ij ≤ 0, ∀l =1, . . . 2nx , ∀k = 1, . . . 2nx, where hk and mk are the 

k-th row of the matrices H = [Id nx , −Id nx ]T , M =[Id nx , Id nx ]T .Proof. Since H′

ij (η′

ij ) = {eij | gk(eij ) = hTk (eij − c′

ij ) −

mTk α′

ij ≤ 0, ∀k = 1 , . . . 2nx}, where gk is linear in eij , and thus convex, then the result follows by Prop. 1. △

Proposition 2. Let a graph G, and a path πij in G, where 

ers ∈ H rs (ηrs ), ∀(r, s ) ∈ ϵ(πij ), for some relative configu-ration Hrs (ηrs ). Let the stacked vector ¯η = [ ηrs ](r,s )∈ϵ(πij )

and a relative configuration Hij (ηij ). Then, the inclusion condition L

(r,s )∈ϵ(πij ) Hrs (ηrs ) ⊆ H ij (ηij ) is equivalent to a set of linear inequalities in ¯η.Proof. (Sketch) By (3), L

(r,s )∈ϵ(πij ) Hrs (ηrs ) =

Hij 

P

(r,s )∈ϵ(πij ) ηrs 



= Hij (η⊕

ij ( ¯ η)) , where 

η⊕

ij ( ¯ η) := P

(r,s )∈ϵ(πij ) ηrs is a linear in each ηrs .By virtue of Corollary 2, we then have that Hij (η⊕

ij ) ⊆Hij (ηij ) ⇔ gk( ¯ η) = hTk (vl(η⊕

ij ( ¯ η)) − cij ) − mTk αij ≤

0, ∀l = 1 , . . . 2nx , ∀k = 1 , . . . 2nx. Since the function 

vl(η⊕

ij ( ¯ η)) is the composition of linear function vl(·) with the linear function η⊕

ij (·), then the inequalities are linear in the variables ¯η. △

REFERENCES [1] G. E. Jan, T. Lei, C.-C. Sun, Z.-Y. You, and C. Luo, “On the problems of drone formation and light shows,” IEEE Trans. Consum. Electron. ,vol. 70, no. 3, pp. 5259–5268, 2024. [2] S. R. Bassolillo, E. D’Amato, M. Mattei, and I. Notaro, “Distributed navigation in emergency scenarios: A case study on post-avalanche search and rescue using drones,” Applied Sciences , vol. 13, no. 20, p. 11186, 2023. [3] O. Maler and D. Nickovic, “Monitoring temporal properties of con-tinuous signals,” in Int. Symp. Formal Tech. Real-Time Fault-Tolerant Syst. Springer, 2004, pp. 152–166. [4] Z. Liu, J. Dai, B. Wu, and H. Lin, “Communication-aware motion planning for multi-agent systems from signal temporal logic specifi-cations,” in Amer. Control Conf. (ACC) , 2017, pp. 2516–2521. [5] A. T. B¨ uy¨ ukkoc ¸ak, D. Aksaray, and Y. Yazicioglu, “Distributed planning of multi-agent systems with coupled temporal logic speci-fications,” in AIAA scitech 2021 forum , 2021, p. 1123. [6] L. Lindemann and D. V. Dimarogonas, Formal Methods for Multi-Agent Feedback Control Systems . MIT Press, 2025. [7] L. Long and G. Gao, “Dynamic event-triggered prescribed perfor-mance control for nonlinear systems with signal temporal logic,” Int. J. Robust Nonlinear Control , vol. 34, no. 7, pp. 4753–4768, 2024. [8] T. Zaccherini, S. Liu, and D. V. Dimarogonas, “Observer-based control of multi-agent systems under stl specifications,” arXiv:2602.05586 .[9] G. Marchesini, S. Liu, L. Lindemann, and D. V. Dimarogonas, “A communication consistent approach to signal temporal logic task decomposition in multi-agent systems,” IEEE Trans. Autom. Control ,pp. 1–16, 2025. [10] S. Liu, A. Saoud, and D. V. Dimarogonas, “Controller synthesis of collaborative signal temporal logic tasks for multiagent systems via assume-guarantee contracts,” IEEE Trans. Autom. Control , vol. 70, no. 9, pp. 5894–5909, 2025. [11] D. Gundana and H. Kress-Gazit, “Event-based signal temporal logic tasks: Execution and feedback in complex environments,” IEEE Robot. Autom. Lett. , vol. 7, no. 4, pp. 10 001–10 008, 2022. [12] M. Chen, Q. Tam, S. C. Livingston, and M. Pavone, “Signal tem-poral logic meets reachability: Connections and applications,” in Int. Workshop Algorithmic Found. Robot. Springer, 2018, pp. 581–601. [13] R. Sedgewick and K. Wayne, Algorithms . Addison-wesley profes-sional, 2011. [14] J. A. Preiss*, W. H¨ onig*, G. S. Sukhatme, and N. Ayanian, “Crazyswarm: A large nano-quadcopter swarm,” in IEEE Int. Conf. Robot. Autom. (ICRA) . IEEE, 2017, pp. 3299–3304. [15] S. Boyd and L. Vandenberghe, Convex optimization . Cambridge university press, 2004.