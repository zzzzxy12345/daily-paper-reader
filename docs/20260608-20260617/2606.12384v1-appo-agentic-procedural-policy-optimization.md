---
title: "APPO: Agentic Procedural Policy Optimization"
title_zh: APPO：代理程序性策略优化
authors: "Xucong Wang, Ziyu Ma, Yong Wang, Yuxiang Ji, Shidong Yang, Guanhua Chen, Pengkun Wang, Xiangxiang Chu"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12384v1"
tags: ["query:agent"]
score: 9.0
evidence: 具有程序化信用分配的智能体强化学习
tldr: 当前智能体强化学习多基于工具调用边界等粗粒度单元进行信用分配，忽略了广泛分布于序列中的关键决策点。本文通过分析发现，令牌熵无法可靠反映决策影响。为此提出APPO方法，将分支和信用分配细化到序列中的决策点，采用分支分数（结合令牌不确定性和后续策略增益）选择探索位置，并利用过程级优势缩放优化信用分配。在13个基准上，APPO较强基线提升近4个点，保持工具调用高效性及行为可解释性。该工作为智能体RL中的细粒度信用分配提供了有效范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12384v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1423, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12384v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1432, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12384v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 235, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12384v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1431, \"height\": 253, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12384v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1418, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12384v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1438, \"height\": 563, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12384v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 957, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12384v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 953, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12384v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 729, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12384v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 674, \"height\": 380, \"label\": \"Table\"}]"
motivation: 现有智能体RL基于粗粒度启发单元分配信用，难以定位影响下游结果的中间决策点。
method: 提出APPO，通过分支分数（令牌不确定性+策略增益）选择细粒度决策点分支，并采用过程级优势缩放分配信用。
result: 在13个基准上，APPO较现有强基线一致提升近4个点，且保持工具调用效率和可解释性。
conclusion: APPO通过细粒度决策点分支与过程级信用分配，提升了智能体RL的性能与探索效率。
---

## 摘要
近期代理强化学习（RL）的进展显著提升了大语言模型智能体的多轮工具使用能力。然而，现有方法大多在粗略的启发式单元（如工具调用边界或固定工作流）上分配信用，使得难以识别哪些中间决策影响下游结果。在这项工作中，我们从两个角度研究代理RL：	extit{何处分支以及分支后如何分配信用}。我们的初步分析表明，有影响力的决策点广泛分布在生成的序列中，而非集中在工具调用处，而仅凭词元熵无法可靠反映其对最终结果的影响。基于这些观察，我们提出	extbf{代理程序性策略优化（APPO）}，它将分支和信用分配从粗略的交互单元转移到序列中的细粒度决策点。APPO使用结合词元不确定性与策略诱导的后续延续似然增益的分支分数来选择分支位置，从而实现更有针对性的探索，同时过滤掉虚假的高熵位置。它还引入过程级优势缩放，以更好地在分支展开中分配信用。在13个基准测试上的实验表明，APPO持续提升强代理RL基线近4个百分点，同时保持高效的工具调用和可解释的行为。

## Abstract
Recent advances in agentic Reinforcement Learning (RL) have substantially improved the multi-turn tool-use capabilities of large language model agents. However, most existing methods assign credit over coarse heuristic units, such as tool-call boundaries or fixed workflows, making it difficult to identify which intermediate decisions influence downstream outcomes. In this work, we study agentic RL from two perspectives: \textit{where to branch and how to assign credit after branching}. Our pilot analysis shows that influential decision points are broadly distributed throughout the generated sequence rather than concentrated at tool calls, while token entropy alone does not reliably reflect their impact on final outcomes. Motivated by these observations, we propose \textbf{Agentic Procedural Policy Optimization (APPO)}, which shifts branching and credit assignment from coarse interaction units to fine-grained decision points in the sequence. APPO selects branching locations using a Branching Score that combines token uncertainty with policy-induced likelihood gains of subsequent continuations, enabling more targeted exploration while filtering out spurious high-entropy positions. It further introduces procedure-level advantage scaling to better distribute credit across branched rollouts. Experiments on 13 benchmarks show that APPO consistently improves strong agentic RL baselines by nearly 4 points, while keeping efficient tool-calls and maintaining behavior interpretability.

---

## 论文详细总结（自动生成）

# APPO：Agentic Procedural Policy Optimization 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有智能体强化学习（Agentic RL）方法在分配信用时通常基于粗粒度的启发式单元，如工具调用边界或固定工作流，导致难以识别哪些中间决策真正影响下游结果（成功或失败）。
- **背景**：大语言模型（LLM）作为自主智能体需要多轮交互和工具使用，基于稀疏结果奖励的 RL 训练（如 RLVR）存在信用分配困难。现有工作通过从中间位置展开分支（branching）来生成多候选轨迹，并以结果差异分配信用，但分支位置仍局限于粗粒度单元（如工作流或工具调用步），忽略了广泛分布于思考过程中的细粒度决策点。
- **初步分析**：作者通过实验发现，高不确定性位置并非集中在工具调用边界，而是分布在思考过程中；同时，单纯的高熵不能可靠指示决策重要性（部分高熵源于词汇稀罕性而非任务相关性）。这说明了需要关注思考过程中的程序性信息，并设计更原则性的分支标准。

## 2. 论文提出的方法论

### 核心思想
- 将分支（branching）和信用分配从粗粒度启发式单元转移到生成序列中的细粒度决策点（procedures）。提出 **Agentic Procedural Policy Optimization (APPO)**，包含：
  1. **程序性分支**：利用分支分数（Branching Score, BS）选择高价值的分支位置。
  2. **过程级优势估计**：通过双组优势计算和未来感知优势项实现精细化的信用分配。

### 关键技术细节
- **分支分数 (BS)**：结合令牌熵 \(H_t\) 和未来价值 \(\Omega_{n,i}\)（基于累积衰减重要性采样比）：
  \[
  \Omega_{n,i} = \exp\left(\sum_{i'\ge i} \gamma^{i'-i} \log \rho_{i'}(\theta)\right)
  \]
  \[
  BS_{n,i} = Z(\text{clip}(\Omega_{n,i}, 1-\epsilon', 1+\epsilon')) \cdot Z(H_{n,i})
  \]
  其中 \(Z\) 是 z-score 归一化，\(\rho\) 是当前策略与旧策略的概率比。BS 选择既不确定又对未来有影响的令牌作为分支点。
- **过程级优势缩放**：
  - 双组优势估计：将初始展开（\(\mathcal{T}_{\text{init}}\)）和分支展开（\(\mathcal{T}_{\text{branch}}\)）分别计算组内相对优势，避免策略分布偏差。
  - 未来感知优势项 \(\hat{A}^{\text{fut}}_{n,i}\)（类似 \(\Omega\) 的剪辑形式），用于对具有更强下游影响的决策赋予更大信用。
  - 最终优势：\(\hat{A}_{n,i} = \hat{A}^{\text{base}}_{n,i} (1 + b \cdot \hat{A}^{\text{fut}}_{n,i})\)。
- **优化目标**：基于 PPO 风格，包含裁剪和 KL 正则项，仅对初始展开进行梯度更新，分支展开仅提供辅助信用信号。

### 算法流程（文字说明）
1. 对每个任务生成 \(N\) 条初始完整轨迹（\(N\) 个根节点）。
2. 在训练 mini-batch 中，用当前策略计算每条轨迹每个令牌的 BS，选择 top-\(B\) 个令牌作为分支点。
3. 从每个选中的分支点重新采样继续生成新的分支轨迹（由当前策略生成，梯度不传播）。
4. 计算所有轨迹的奖励，对初始轨迹和分支轨迹分别计算组内相对优势。
5. 结合未来感知优势项，得到最终优势。
6. 使用 PPO 更新策略（仅对初始轨迹计算损失）。

### 理论贡献
- **定理 3.1**：BS 引导的分支相比均匀分支能够降低梯度方差（方差缩减）。
- **定理 3.2**：在过程级分支和优势设计下，保证策略改进下界成立。

## 3. 实验设计

### 数据集与场景
- **数学推理**：AIME24、AIME25、MATH500、GSM8K、MATH。
- **知识密集型推理**：HotpotQA、2WikiMultihopQA、Musique、Bamboogle、WebWalker。
- **深度搜索**：GAIA（三级难度）、Humanity's Last Exam (HLE)、Xbench。

### Benchmark 框架
使用 F1-score（对于4个 QA 任务）和 LLM-as-a-Judge（Qwen2.5-72B-Instruct 评估正确性）作为 pass@1 指标。

### 对比方法
- **经典 RL**：GRPO、Reinforce++、DAPO、GPPO、CISPO。
- **智能体 RL**：GiGPO、ARPO。
- **其他参考**：Zero-shot、TIR Prompting、RAG、Search-o1、WebThinker、ReAct 等。
- **主干模型**：Llama3.1-8B-Instruct、Qwen2.5-7B-Instruct、Qwen3-8B/14B；另外报告了 QwQ-32B、DeepSeek-R1、GPT-4o、o1-preview 等大模型作为参考。

## 4. 资源与算力

- **SFT 训练**：使用 LLAMA-Factory，8 GPU（未明确型号，可能为 A100 或 H100，但未说明）。
- **RL 训练**：
  - 7B 模型：8 张 NVIDIA H100 GPU。批次大小 128，PPO mini-batch 16，每个任务 2 个 epoch（深度搜索任务 5 个 epoch）。
  - 14B 模型：16 张 NVIDIA H100 GPU，其余超参数相似。
- **框架**：基于 VERL（RL 框架），使用 FlashAttention-2、DeepSpeed ZeRO-3。
- 文中未报告具体训练时长，但提供了批次和更新步数信息（128 batch，8次梯度更新/步）。

## 5. 实验数量与充分性

- **主实验**：在 13 个基准上进行，表 1 展示了在 10 个推理任务上的结果（两个骨干模型），表 2 展示了在 4 个深度搜索任务上的结果（两个模型规模，包含多个对比方法）。
- **Pass@K 分析**：在 GAIA、WebWalkerQA 等任务上分析 pass@1 到 pass@5 的扩展表现，显示 APPO 改善整体候选分布。
- **分支配置消融**（表 3）：分析 \(N\)（初始轨迹数）和 \(B\)（每个轨迹分支点数量）的不同配置，验证平衡配置最优。
- **组件消融**（表 4）：分别移除 BS（改用熵）、去掉未来感知优势、去掉双组优势，验证各组件贡献。
- **训练动态**（图 4）：对比 APPO 与 ARPO 的奖励曲线。
- **多样性分析**（图 5）：UMAP 聚类显示 APPO 的分支更紧凑、结构更清晰。
- **BS 指标可视化**（图 6）：词云展示高熵令牌 vs BS 选择令牌，说明 BS 过滤稀罕名词。
- **总结**：实验覆盖多种任务、模型尺寸、超参数、消融，统计充分、客观公平。结果报告了平均值和 pass@K，避免了偶然性担忧。

## 6. 论文的主要结论与发现

- APPO 在 13 个基准上一致优于现有强基线（如 ARPO、GRPO），平均提升近 4 个百分点（表 1 表格上标有 +7.9% 对比 baselines）。
- 细粒度决策点分支比粗粒度（工具调用或工作流）更有效，BS 指标优于单纯熵。
- 过程级优势缩放（双组+未来感知）进一步优化信用分配。
- APPO 不仅提升 top-1 性能，还扩大 pass@K 优势，暗示其改善了候选解的多样性。
- 训练动态更稳定，奖励提升更平滑。
- 模型生成的分支具有更清晰的语义结构。

## 7. 优点

- **方法创新**：首次将分支和信用分配细化到程序性决策点（非工具调用边界），并提出综合分支分数（BS）结合熵与未来价值，有效过滤虚假高熵。
- **理论支撑**：证明了方差缩减和策略改进下界，增强了方法的可信度。
- **实验全面**：覆盖多种任务类型（数学、知识检索、深度搜索）、多种骨干模型（7B/8B/14B），并进行大量消融和扩展分析。
- **效率与可解释性**：保持与基线相似的工具调用次数，BS 选择的可视化展示可解释性。
- **泛化性**：在两个不同系列模型（Llama、Qwen）上均取得一致提升，说明算法通用性。

## 8. 不足与局限

- **理论最优性不足**：BS 指标缺乏严格的证明表明其是最优分支标准，仅凭实验验证。
- **工具限制**：仅使用了 Search 和 Python 两种工具，未扩展到更丰富的应用工具集（如文件操作、API 调用等）。
- **多层分支实验初步**：当分支层数 L>1 时，性能不如单层分支配合多初始轨迹，说明当前设计倾向于广度优先；多层树策略有待进一步探索。
- **偏差风险**：双组优势计算假设分支策略与初始策略分布不同，但通过分离计算缓解了偏差；然而，分支由当前策略生成且不传播梯度，可能引入某些隐式分布偏移。
- **应用限制**：方法依赖于能够生成多样化分支的模型，对于本身输出多样性低的模型可能效果有限。
- **算力成本**：尽管相比其他方法效率不错，但仍需要多卡 GPU 训练（8-16 H100），或许对资源受限团队构成挑战。

（完）
