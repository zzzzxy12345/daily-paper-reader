---
title: "Dreaming Of Others: Latent Teammate Modeling In World Models For Multi-Agent Reinforcement Learning"
title_zh: 想象他人：多智能体强化学习中世界模型内的潜在队友建模
authors: Tomas Leroy-Stone
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31361v1"
tags: ["query:agent"]
score: 9.0
evidence: 多智能体强化学习中基于世界模型的潜在队友建模
tldr: 在合作多智能体强化学习中，队友的内在策略和意图不可直接观测，现有世界模型（如Dreamer）因无法处理队友引起的不确定性而受限。本文提出将队友视为结构化的可学习组件，在Dreamer风格的RSSM中将潜在状态分解为环境和队友部分，并学习辅助的Theory-of-Mind头部来从部分轨迹推断队友的潜在嵌入（如角色、意图和预测动作）。这些队友潜在变量条件化actor和critic，使代理能够在想象中适应多样化合作者。该方法支持部分可观测下的零样本和少样本协调，并通过基准和评估协议验证其效果，将世界模型从环境预测拓展为社会行为模拟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31361v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1254, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31361v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1097, \"height\": 714, \"label\": \"Figure\"}]"
motivation: 现有世界模型无法处理多智能体场景中由队友引起的不确定性，限制了其在合作MARL中的泛化和样本效率。
method: 将Dreamer的RSSM潜在状态分解为环境和队友组件，并附加Theory-of-Mind头部推断队友的行为嵌入。
result: 代理能利用队友潜在变量进行想象和适应，支持零样本和少样本协调，在部分可观测环境中表现更优。
conclusion: 世界模型可作为社会行为模拟器，推动通用和人类兼容的多智能体AI发展。
---

## 摘要
在合作多智能体强化学习（MARL）中，智能体必须与内部策略和意图不可直接观察的伙伴协调。虽然像Dreamer这样的世界模型在单智能体场景中展现了强大的泛化能力和样本效率，但其在MARL中的应用仍因无法处理队友引发的不确定性而受限。我们提出一种新视角：将队友视为智能体世界模型中结构化、可学习的组件。我们引入一种架构，将Dreamer风格循环状态空间模型（RSSM）的潜在状态分解为环境组件和队友组件，并学习一个辅助心智理论（ToM）头，以从部分轨迹中推断伙伴行为（如角色、意图和预测动作）的潜在嵌入。这些队友潜在表示影响演员和评论家，使智能体能够想象并适应多样的协作者。我们概述了该方法如何在部分可观察场景中支持零样本和少样本协调，并提出了一套基准和评估协议以评估其影响。这项工作将世界模型定位为不仅是环境动态的预测器，更是社会行为的模拟器，为可泛化、与人类兼容的人工智能开辟了新方向。

## Abstract
In cooperative multi-agent reinforcement learning (MARL), agents must coordinate with partners whose internal policies and intentions are not directly observable. While world models such as Dreamer have demonstrated strong generalization and sample efficiency in single-agent settings, their application to MARL remains limited by an inability to handle teammate-induced uncertainty. We propose a new perspective: treat teammates as structured, learnable components within the agent's world model. We introduce an architecture that factorizes the latent state of a Dreamer-style recurrent state-space model (RSSM) into environment and teammate components, and learns an auxiliary Theory-of-Mind (ToM) head to infer latent embeddings of partner behavior such as character, intent, and predicted actions from partial trajectories. These teammate latents condition the actor and critic, enabling the agent to imagine and adapt to diverse collaborators. We outline how this approach can support zero-shot and few-shot coordination in partially observable settings and propose a set of benchmarks and evaluation protocols to assess its impact. This work positions world models as not only predictors of environmental dynamics, but as simulators of social behavior, opening new directions for generalizable, human-compatible AI.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：在合作多智能体强化学习（MARL）中，队友的行为策略和意图对单个智能体而言是不可直接观测的，这导致了环境非平稳性（non-stationarity）和部分可观测性（partial observability）。现有世界模型（如Dreamer）在单智能体场景中表现出色，但直接应用于MARL时，它们将队友行为压缩为未区分的噪声，无法处理队友引发的不确定性，从而限制了泛化和样本效率。
- **整体含义**：作者提出将队友视为世界模型内**结构化、可学习的潜在组件**，而非随机噪声。这一视角使世界模型不仅预测环境动态，还能**模拟社会行为**，从而支持智能体在想象中适应多样化的协作者。最终目标是推动通用、与人类兼容的多智能体AI发展，尤其是在零样本和少样本协调场景中。

## 2. 论文提出的方法论

### 核心思想
- 在Dreamer风格的RSSM（循环状态空间模型）中，将潜在状态 `z_t` 分解为两个因子：
  - `z_t^{env}`：捕捉环境物理动态。
  - `z_t^{team}`：编码推断出的队友行为。
- 附加一个**Theory-of-Mind（ToM）头部**，从部分观察轨迹中推断队友的潜在嵌入（如角色、意图、预测动作）。
- 这些队友潜在表示条件化actor和critic，使智能体在想象 rollout 中能够模拟合作伙伴的变异性，从而实现零样本和少样本适应。

### 关键技术细节
1. **编码器**：在每个时间步t，编码器处理受控智能体的观察 `x_t` 和动作 `a_t^0`，生成确定性隐藏状态 `h_t`。
2. **因子化潜在变量**：`z_t = [z_t^{env}, z_t^{team}]`，每个部分通过独立的分布采样。
3. **解码器**：
   - 观察解码器：从 `z_t^{env}` 重建 `̂x_t`。
   - 队友策略解码器：从 `z_t^{team}` 预测队友下一动作的分布 `̂π_t^j(·)`。
4. **转移模型**：所有智能体的动作 `(a_t^0, a_t^j)` 共同驱动状态转移到 `h_{t+1}`。
5. **ToM 损失函数**（公式1）：
   ```math
   L_{ToM} = E_t[ -∑_a π_t^j(a) log ̂π_t^j(a) ] + α KL( q(z_t^{team}|h_t) || p(z_t^{team}|h_{t-1}, a_{t-1}) )
   ```
   - 第一项：校准交叉熵，使预测分布匹配真实队友动作分布（例如one-hot标签）。
   - 第二项：KL散度，强制队友潜在变量在时间上保持一致性。
6. **训练**：在标准Dreamer世界模型和actor-critic损失之上，增加 `λ_{ToM} L_{ToM}` 作为额外目标。想象 rollout 时，采样 `z_t^{team}` 以模拟不同队友行为。
7. **部署与适应**：测试时在线推断 `z_t^{team}`，无需访问队友策略或观察，可用于零样本协调，并随着交互增多实现少样本改进。

## 3. 实验设计

- **论文性质说明**：本文为**提案（proposal）论文**，未报告任何实证结果。作者仅提出了**评估协议（Evaluation Protocol）**，未实际运行实验。
- **提出的基准环境**：
  - **Multi-Agent Particle Environments (MPE)**：轻量级、可控场景，用于诊断模型是否学会可区分的队友潜在变量，并分离社会与物理动态。
  - **Overcooked-AI**：主要零样本协调测试床，使用标准伙伴划分来评估推断的队友嵌入是否能支持与未见协作者的即时协调。
  - **Melting Pot**：大规模环境，评估跨多样社会背景和伙伴群体的鲁棒性，考察方法如何处理规范和惯例的分布偏移。
- **提议的对比基线**：未明确指定，但暗示应与标准MARL方法（如MAPPO、QMIX）以及现有基于世界模型的MARL方法（如MA-Dreamer、CoDreamer、GAWM）进行比较。
- **提议的评估指标**：回合回报、样本效率、零样本协调得分、少样本改进幅度、跨博弈鲁棒性。

## 4. 资源与算力

- **文中未提及**：由于是纯提案论文，没有进行实际实验，因此未报告任何GPU型号、数量、训练时长或计算资源消耗。作者仅描述了概念和设计，未涉及具体实现与训练。

## 5. 实验数量与充分性

- **实验数量**：0（无实验）。
- **充分性评价**：本文为立场/概念论文，旨在提出问题并描述架构，不包含实验验证。因此无法评估实验的充分性、客观性或公平性。作者在文中明确声明“This paper is a proposal intended to catalyze discussion… we do not report empirical results.” 评估部分仅作为对未来工作的提议。

## 6. 论文的主要结论与发现

- 世界模型可以扩展为“社会行为模拟器”，通过将队友建模为结构化的潜在过程，而不是噪声，可以减少MARL中的非平稳性。
- 因子化潜在状态和ToM头部的设计可以使智能体在想象中适应看不见的协作者，支持零样本和少样本协调。
- 所提出的架构不需要集中训练、显式通信或访问队友策略，这使其更接近真实的人类-AI协作场景。
- 该方法有望提高可解释性（队友潜在变量具有语义含义）以及与人类伙伴的兼容性。

## 7. 优点

- **概念新颖**：将队友建模问题融入世界模型框架，为MARL中非平稳性问题提供了新的解决思路。
- **通用性**：不依赖于集中训练、共享潜在状态或显式通信，适用于部分可观测和去中心化执行。
- **可扩展**：基于Dreamer家族，继承了其样本效率和泛化能力，通过增加一个简单头部即可实现，计算开销较小。
- **与实际应用对齐**：强调人类兼容性，关注零样本和少样本协调，符合当前人机协作研究热点。
- **评估协议全面**：规划了从简单诊断性环境（MPE）到复杂社会场景（Melting Pot）的分层测试，并涵盖了多种指标。

## 8. 不足与局限

- **缺乏实证验证**：论文仅提出架构和想法，没有任何实验数据或结果来证明其有效性。所有声称的益处（如减少非平稳性、提升零样本协调）均为假设，需要未来工作证实。
- **技术细节不够充分**：例如队友潜在变量的维度设计、采样策略、损失函数中α和λ的调参规则等均未展开；与Dreamer原有组件的具体集成方式也较模糊。
- **未讨论失败场景**：例如当队友行为高度非平稳或恶意时，模型是否会崩溃；队友潜在变量是否能真正捕捉意图 vs 仅仅过拟合表面模式。
- **可能偏差风险**：作者未讨论数据收集中的偏差（例如训练时队友策略的分布是否覆盖测试场景），也未分析计算成本或可复现性。
- **基线比较缺失**：仅提及要比较的方法，但没有提供任何定量或定性比较，使读者难以判断新方法的相对优劣。
- **应用限制**：依赖于Dreamer的RSSM框架，该框架本身对复杂高维视觉输入的计算开销较高，在多智能体场景中可能面临扩展性问题。此外，需要队友动作作为监督信号，在无法观测到队友动作的场景中不适用。

（完）
